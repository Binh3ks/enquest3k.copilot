# Proxy audio requests to Cloudflare R2 CDN
/audio/* https://pub-8f917d02000c4be2a7214afb8d12abd3.r2.dev/audio/:splat 200

# Explicitly serve static image files (before SPA fallback)
/images/* /images/:splat 200

# Explicitly serve avatar gallery SVGs as static files (before SPA fallback)
/avatars/* /avatars/:splat 200

# Prevent SPA fallback for missing JS/CSS assets (return proper 404 instead of index.html 200)
/assets/* /assets/:splat 404

# SPA fallback
/* /index.html 200
