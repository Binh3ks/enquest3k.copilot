# ENGQUEST MASTER PROMPT V27.1 - PRODUCTION READY
## Complete Code Generation Protocol for Mass Production (All 156 Weeks)

**Version**: 27.1 (Together AI Integration + Mission 2 Family Fix)
**Status**: PRODUCTION READY
**Golden Standard**: Week 1 & Week 2 with AI Tutor Complete
**Last Updated**: 16/01/2026

**Critical Changes from V27.0**:
- ✅ **Together AI** integrated as Layer 1 provider (60 req/min free tier)
- ✅ **Mission 2 Family Focus** fixed to ask about both mother AND father
- ✅ **JSON Response Format** enforced for all AI providers
- ✅ **Three-Layer Fallback**: Together AI → Groq → Gemini → Deterministic
- ⚠️ **BREAKING**: Groq moved from Layer 1 to Layer 2 (rate limit issues)

---

## 🚨 CRITICAL UPDATES FOR MASS PRODUCTION

### A. AI Provider Architecture Changes (Jan 16, 2026)

#### **Problem Solved**: Groq Rate Limiting Crisis
- **Issue**: Groq free tier (15 req/min) was insufficient for 100 concurrent students
- **Impact**: Every request hit 429 errors immediately, blocking all AI interactions
- **Solution**: Integrated Together AI (60 req/min) as primary provider

#### **New Three-Layer Fallback Chain**

```
Layer 1: Together AI (meta-llama/Llama-3.3-70B-Instruct-Turbo) 🆕
├─ Speed: Fast (~1-1.5s)
├─ Quota: 60 requests/minute FREE (4x better than Groq)
├─ Tokens: 1M tokens/month FREE
├─ API: OpenAI-compatible
├─ Response Format: JSON enforced with response_format parameter
└─ On Error (400/429/500) → Layer 2

Layer 2: Groq (llama-3.3-70b-versatile) [MOVED FROM LAYER 1]
├─ Speed: Ultra-fast (<500ms)
├─ Quota: 14 requests/minute (safety buffer for 15/min limit)
├─ Backoff: 2s exponential (2→4→8→10s max)
└─ On Error → Layer 3

Layer 3: Gemini 2.0 Flash [UNCHANGED]
├─ Speed: Fast (~1-2s)
├─ Quota: 60 requests/minute
└─ On Error → Layer 4

Layer 4: Deterministic Fallback [UNCHANGED]
└─ Safe generic responses when all APIs fail
```

---

### B. Environment Variables Setup

**File**: `.env` (root directory)

**Required API Keys**:
```bash
# Layer 1: Together AI (PRIMARY - 60 req/min)
VITE_TOGETHER_API_KEY=your_together_api_key_here

# Layer 2: Groq (BACKUP - 14 req/min)
VITE_GROQ_API_KEY=your_groq_api_key_here

# Layer 3: Gemini (FINAL FALLBACK - 60 req/min)
VITE_GEMINI_API_KEY=your_gemini_api_key_here
```

**How to Get API Keys**:
1. **Together AI**: https://api.together.xyz/
   - Sign up with email
   - Free tier: 1M tokens/month, 60 req/min
   - Copy API key from dashboard

2. **Groq**: https://console.groq.com/
   - Free tier: 15 req/min
   - Already configured

3. **Gemini**: https://aistudio.google.com/apikey
   - Free tier: 60 req/min
   - Already configured

---

### C. AI Provider Configuration

**File**: `src/services/ai_tutor/aiRouter.js`

#### 1. Provider Constants

```javascript
const PROVIDERS = {
  together: {
    enabled: !!import.meta.env.VITE_TOGETHER_API_KEY,
    model: 'meta-llama/Llama-3.3-70B-Instruct-Turbo',
    maxTokens: 1000,
    temperature: 0.7,
    endpoint: 'https://api.together.xyz/v1/chat/completions'
  },
  groq: {
    enabled: !!import.meta.env.VITE_GROQ_API_KEY,
    model: 'llama-3.3-70b-versatile',
    maxTokens: 1000,
    temperature: 0.7,
    endpoint: 'https://api.groq.com/openai/v1/chat/completions'
  },
  gemini: {
    enabled: !!import.meta.env.VITE_GEMINI_API_KEY,
    model: 'gemini-2.0-flash-exp',
    maxTokens: 1000,
    temperature: 0.7
  }
};
```

#### 2. Together AI Implementation

**Critical Function**: `callTogether(messages, systemPrompt, options)`

```javascript
async function callTogether(messages, systemPrompt, options = {}) {
  if (!PROVIDERS.together.enabled) {
    throw new Error('Together AI API key not configured');
  }
  
  const startTime = Date.now();
  let requestBody;
  
  try {
    // Normalize messages to OpenAI format
    const normalizedMessages = messages.map(msg => ({
      role: msg.role === 'model' ? 'assistant' : msg.role,
      content: msg.content || msg.text || ''
    })).filter(msg => msg.content && msg.content.trim().length > 0);

    // Separate system prompt
    const systemMessage = normalizedMessages.find(m => m.role === 'system');
    const otherMessages = normalizedMessages.filter(m => m.role !== 'system');
    
    const finalSystemPrompt = systemMessage ? systemMessage.content : (systemPrompt || '');

    requestBody = {
      model: PROVIDERS.together.model,
      messages: [
        { role: 'system', content: finalSystemPrompt },
        ...otherMessages
      ],
      max_tokens: options.maxTokens || PROVIDERS.together.maxTokens,
      temperature: options.temperature || PROVIDERS.together.temperature,
      response_format: { type: "json_object" }  // 🔑 CRITICAL: Force JSON output
    };
    
    const response = await axios.post(TOGETHER_ENDPOINT, requestBody, {
      headers: {
        'Authorization': `Bearer ${TOGETHER_API_KEY}`,
        'Content-Type': 'application/json'
      },
      timeout: 10000
    });
    
    const elapsed = Date.now() - startTime;
    
    // Parse response
    let responseData = response.data;
    if (response.data?.choices?.[0]?.message?.content) {
      const rawContent = response.data.choices[0].message.content;
      
      // Try to parse as JSON first
      try {
        // Remove markdown code blocks and clean up
        let cleanedContent = rawContent.trim();
        cleanedContent = cleanedContent.replace(/```json\n?/g, '').replace(/\n?```/g, '');
        cleanedContent = cleanedContent.trim();
        
        responseData = JSON.parse(cleanedContent);
        console.log('✅ Together AI JSON parsed successfully');
      } catch (e) {
        // Model returned plain text instead of JSON - let responseParser handle it
        console.warn('⚠️ Together AI returned plain text, not JSON. Passing to responseParser.');
        responseData = rawContent;
      }
    }

    console.log(`✅ Together AI success in ${elapsed}ms`);
    return responseData;
    
  } catch (error) {
    const elapsed = Date.now() - startTime;
    console.error(`❌ Together AI error in ${elapsed}ms:`, error.response?.status, error.message);
    
    if (error.response?.status === 400) {
      console.error('🔍 Together AI 400 Debug:', {
        model: PROVIDERS.together.model,
        messageCount: messages.length,
        errorData: error.response?.data
      });
    }
    
    throw error;
  }
}
```

**Key Implementation Details**:
1. **response_format: { type: "json_object" }** - Forces model to ALWAYS return valid JSON
2. **Error Handling** - Catches JSON parse errors gracefully, falls back to responseParser
3. **Message Normalization** - Converts Gemini-style messages to OpenAI format
4. **Timeout** - 10s limit to prevent hanging requests
5. **Debug Logging** - Clear console logs for production monitoring

#### 3. Updated sendToAI() Routing Logic

```javascript
export async function sendToAI(messages, systemPrompt = '', options = {}) {
  const startTime = Date.now();
  let lastError = null;

  // Layer 1: Together AI (60 req/min) 🆕
  if (PROVIDERS.together.enabled) {
    try {
      console.log('🚀 Layer 1: Trying Together AI (attempt 1/2)...');
      const response = await callTogether(messages, systemPrompt, options);
      const parsed = parseAIResponse(response);
      console.log(`✅ Together AI succeeded in ${Date.now() - startTime}ms`);
      return parsed;
    } catch (error) {
      console.warn('⚠️ Together AI failed:', error.message);
      lastError = error;
      console.log('🔄 Fallback to Layer 2: Groq...');
    }
  }

  // Layer 2: Groq (14 req/min) [MOVED FROM LAYER 1]
  if (PROVIDERS.groq.enabled) {
    try {
      console.log('🚀 Layer 2: Trying Groq...');
      const response = await callGroq(messages, systemPrompt, options);
      const parsed = parseAIResponse(response);
      console.log(`✅ Groq succeeded (fallback) in ${Date.now() - startTime}ms`);
      return parsed;
    } catch (error) {
      console.warn('⚠️ Groq failed:', error.message);
      lastError = error;
      console.log('🔄 Fallback to Layer 3: Gemini...');
    }
  }

  // Layer 3: Gemini (60 req/min)
  if (PROVIDERS.gemini.enabled) {
    try {
      console.log('🚀 Layer 3: Trying Gemini...');
      const response = await callGemini(messages, systemPrompt, options);
      const parsed = parseAIResponse(response);
      console.log(`✅ Gemini succeeded (fallback) in ${Date.now() - startTime}ms`);
      return parsed;
    } catch (error) {
      console.warn('⚠️ Gemini failed:', error.message);
      lastError = error;
    }
  }

  // Layer 4: Deterministic Fallback
  console.error('❌ All AI providers failed, using deterministic fallback');
  const fallbackResponse = generateContextualFallback(/* ... */);
  return parseAIResponse(fallbackResponse);
}
```

---

### D. Mission 2 Family Focus Fix (Critical for Mass Production)

#### **Problem**: Mission 2 Only Asked About Mother

**Before** (Week 2 Mission 2):
```javascript
const family_2 = {
  mother_morning: {
    canonical: "What does your mother do in the morning?",
    hints: ["She", "wakes", "cooks", "works", "cleans"]
  },
  mother_breakfast: {
    canonical: "Does your mother cook breakfast?",
    hints: ["Yes", "she", "cooks", "breakfast", "eggs"]
  },
  // ... 13 more mother-only questions
};
```

**Issue**: Students couldn't practice talking about their father

**After** (Fixed):
```javascript
const family_2 = {
  mother_morning: {
    canonical: "What does your mother or father do in the morning?",
    hints: ["She", "He", "wakes", "cooks", "works", "cleans"]
  },
  mother_breakfast: {
    canonical: "Does your mother or father cook breakfast?",
    hints: ["Yes", "she", "he", "cooks", "breakfast", "eggs"]
  },
  mother_busy: {
    canonical: "Is your mother or father busy?",
    hints: ["Yes", "she", "he", "is", "busy", "works"]
  },
  mother_work: {
    canonical: "Where does your mother or father work?",
    hints: ["She", "He", "works", "at", "home", "office"]
  },
  // ... all 15 steps updated to include father
};
```

**Changes Applied**:
1. ✅ All canonical questions now say "mother or father" (not "mother")
2. ✅ All hints arrays include both "She" and "He" pronouns
3. ✅ Greeting updated: "Hi! Tell me about your mother OR FATHER."
4. ✅ Title remains "My Mother's Day" (syllabus requirement)
5. ✅ 15 steps total maintained (syllabus requirement)

**File**: `src/services/ai_tutor/turnManager.js` (lines 82-241)

**Mass Production Rule**:
- **Week 2 Mission 1**: About student's family (general)
- **Week 2 Mission 2**: About mother OR father (parent's day)
- **Week 2 Mission 3**: About siblings (brothers/sisters)
- When creating Week 2 for future classes, ALWAYS include "or father" in Mission 2

---

## 🔧 TECHNICAL IMPLEMENTATION CHECKLIST

### For Each New Week Production:

#### 1. AI Tutor File (`week_XX_real.js`)

**Import Statement**:
```javascript
import { sendToAI } from '../../services/ai_tutor/aiRouter.js';
```

**Response Format Validation**:
```javascript
// In responseParser.js - V27 Format Detection
const isV27Format = parsed.teacher_ack !== undefined || 
                    parsed.teacher_question !== undefined;

if (isV27Format) {
  console.log("✨ V27 FORMAT DETECTED");
  return {
    ack: parsed.teacher_ack || "",
    recast: parsed.teacher_recast || "",
    encouragement: parsed.teacher_encouragement || "",
    question: parsed.teacher_question || "",
    hints: parsed.suggested_hints || [],
    mission_status: parsed.mission_status || "continue",
    current_turn: parsed.current_turn || null,
    total_turns: parsed.total_turns || null
  };
}
```

#### 2. Environment Setup Check

**Pre-deployment Script**:
```bash
#!/bin/bash
# File: tools/check_env.sh

echo "🔍 Checking AI Provider Configuration..."

if [ -z "$VITE_TOGETHER_API_KEY" ]; then
  echo "❌ Together AI key missing (Layer 1)"
  exit 1
fi

if [ -z "$VITE_GROQ_API_KEY" ]; then
  echo "⚠️ Groq key missing (Layer 2 - optional but recommended)"
fi

if [ -z "$VITE_GEMINI_API_KEY" ]; then
  echo "⚠️ Gemini key missing (Layer 3 - optional but recommended)"
fi

echo "✅ Environment configuration valid"
```

#### 3. Rate Limit Monitoring

**Console Logs to Watch**:
```
✅ Together AI succeeded in 1043ms        # Normal (Layer 1 working)
⚠️ Together AI failed: 429               # Rate limit hit
🔄 Fallback to Layer 2: Groq...          # Automatic fallback
✅ Groq succeeded (fallback) in 723ms    # Layer 2 working
```

**Production Thresholds**:
- Together AI: 60 req/min → ~180 concurrent students (3 req/student/min)
- Groq: 14 req/min → ~40 concurrent students (backup)
- Gemini: 60 req/min → ~180 concurrent students (final fallback)
- **Total Capacity**: 400+ concurrent students with all 3 layers

---

## 📋 MASS PRODUCTION UPDATES

### A. Updated Quality Checklist

**File**: `MASS_PRODUCTION_CHECKLIST.md`

Add new section:

```markdown
### 🤖 AI Provider Configuration (New - Jan 16, 2026)

#### Pre-Production Checks:
- [ ] Together AI API key configured in .env (Layer 1 - PRIMARY)
- [ ] Groq API key configured in .env (Layer 2 - BACKUP)
- [ ] Gemini API key configured in .env (Layer 3 - FALLBACK)
- [ ] Test API calls: `node tools/test_ai_providers.js`
- [ ] Verify response_format JSON enforcement

#### Family Mission Checks (Week 2 Only):
- [ ] Mission 1: General family (brothers, sisters, parents)
- [ ] Mission 2: "mother OR father" in ALL questions ✅
- [ ] Mission 2: Both "She" and "He" in hints arrays ✅
- [ ] Mission 3: Siblings (brothers/sisters)

#### Response Format Validation:
- [ ] All AI responses return V27 format:
  ```json
  {
    "teacher_ack": "Great!",
    "teacher_recast": "Your mother cooks!",
    "teacher_question": "Does she cook breakfast?",
    "suggested_hints": ["Yes", "she", "cooks"],
    "mission_status": "continue"
  }
  ```
- [ ] No plain text responses (Together AI enforces JSON)
- [ ] No markdown code blocks in responses
```

### B. Testing Script

**New File**: `tools/test_ai_providers.js`

```javascript
import axios from 'axios';
import dotenv from 'dotenv';
dotenv.config();

const TOGETHER_KEY = process.env.VITE_TOGETHER_API_KEY;
const GROQ_KEY = process.env.VITE_GROQ_API_KEY;
const GEMINI_KEY = process.env.VITE_GEMINI_API_KEY;

async function testTogetherAI() {
  console.log('\n🧪 Testing Together AI...');
  
  try {
    const response = await axios.post('https://api.together.xyz/v1/chat/completions', {
      model: 'meta-llama/Llama-3.3-70B-Instruct-Turbo',
      messages: [
        { role: 'system', content: 'You are a test assistant. Respond with JSON.' },
        { role: 'user', content: 'Say hello in JSON format with a "message" field.' }
      ],
      response_format: { type: "json_object" },
      max_tokens: 100
    }, {
      headers: {
        'Authorization': `Bearer ${TOGETHER_KEY}`,
        'Content-Type': 'application/json'
      }
    });
    
    const content = response.data.choices[0].message.content;
    const parsed = JSON.parse(content);
    
    console.log('✅ Together AI working');
    console.log('📝 Response:', parsed);
    return true;
  } catch (error) {
    console.error('❌ Together AI failed:', error.message);
    return false;
  }
}

async function testGroq() {
  console.log('\n🧪 Testing Groq...');
  
  try {
    const response = await axios.post('https://api.groq.com/openai/v1/chat/completions', {
      model: 'llama-3.3-70b-versatile',
      messages: [
        { role: 'system', content: 'You are a test assistant.' },
        { role: 'user', content: 'Say hello' }
      ],
      max_tokens: 50
    }, {
      headers: {
        'Authorization': `Bearer ${GROQ_KEY}`,
        'Content-Type': 'application/json'
      }
    });
    
    console.log('✅ Groq working');
    console.log('📝 Response:', response.data.choices[0].message.content);
    return true;
  } catch (error) {
    console.error('❌ Groq failed:', error.message);
    return false;
  }
}

async function testAll() {
  console.log('🚀 Testing AI Providers...\n');
  
  const results = {
    together: await testTogetherAI(),
    groq: await testGroq()
  };
  
  console.log('\n📊 Results:');
  console.log(`  Layer 1 (Together AI): ${results.together ? '✅' : '❌'}`);
  console.log(`  Layer 2 (Groq):        ${results.groq ? '✅' : '❌'}`);
  
  if (!results.together) {
    console.log('\n⚠️ WARNING: Primary AI provider (Together AI) is not working!');
    process.exit(1);
  }
  
  console.log('\n✅ AI provider configuration validated');
}

testAll();
```

**Usage**:
```bash
node tools/test_ai_providers.js
```

---

## 🎯 PRODUCTION DEPLOYMENT CHECKLIST

### Pre-Deployment (For All New Weeks):

1. **API Keys**:
   - [ ] Together AI key added to production .env
   - [ ] Groq key verified (backup)
   - [ ] Gemini key verified (fallback)

2. **Code Verification**:
   - [ ] aiRouter.js has callTogether() function
   - [ ] response_format: { type: "json_object" } in requestBody
   - [ ] Three-layer fallback chain implemented
   - [ ] Error handling for JSON parse failures

3. **Week 2 Specific**:
   - [ ] Mission 2 questions say "mother OR father"
   - [ ] Mission 2 hints include both "She" and "He"
   - [ ] Greeting updated in turnManager.js

4. **Testing**:
   - [ ] Run: `node tools/test_ai_providers.js`
   - [ ] Open Mission 2, click hint button
   - [ ] Verify console shows "Layer 1: Together AI"
   - [ ] Verify response time < 2s
   - [ ] Verify no 429 errors for 10+ consecutive requests

5. **Monitoring**:
   - [ ] Check browser console for AI layer logs
   - [ ] Monitor response times (should be ~1s avg)
   - [ ] Watch for automatic fallbacks (should be rare)

---

## 📚 APPENDIX: API REFERENCE

### Together AI Endpoints

**Base URL**: `https://api.together.xyz/v1`

**Models Available**:
- `meta-llama/Llama-3.3-70B-Instruct-Turbo` (recommended)
- `meta-llama/Meta-Llama-3.1-70B-Instruct-Turbo`

**Rate Limits** (Free Tier):
- 60 requests/minute
- 1M tokens/month
- No credit card required

**Response Format**:
```json
{
  "id": "string",
  "choices": [
    {
      "message": {
        "role": "assistant",
        "content": "{\"teacher_ack\":\"Great!\",...}"
      }
    }
  ],
  "usage": {
    "prompt_tokens": 123,
    "completion_tokens": 45,
    "total_tokens": 168
  }
}
```

### Groq Endpoints (Unchanged)

**Base URL**: `https://api.groq.com/openai/v1`

**Models**: `llama-3.3-70b-versatile`

**Rate Limits**: 15 req/min (free tier)

### Gemini Endpoints (Unchanged)

**Base URL**: `https://generativelanguage.googleapis.com/v1beta`

**Models**: `gemini-2.0-flash-exp`

**Rate Limits**: 60 req/min (free tier)

---

## 🔄 MIGRATION GUIDE (V27.0 → V27.1)

### For Existing Projects:

1. **Update .env**:
   ```bash
   # Add to .env file
   VITE_TOGETHER_API_KEY=your_together_api_key_here
   ```

2. **Update aiRouter.js**:
   - Copy new `callTogether()` function
   - Update PROVIDERS config to include Together AI
   - Modify sendToAI() to call Together AI first
   - Add response_format parameter

3. **Update Week 2 turnManager.js** (if exists):
   - Change all "mother" to "mother or father" in family_2 steps
   - Add "He" to all hints arrays alongside "She"
   - Update greeting message

4. **Test**:
   ```bash
   npm run dev
   # Open Week 2 Mission 2
   # Click hints and verify Together AI logs
   ```

5. **Deploy**:
   ```bash
   npm run build
   # Deploy to production
   ```

---

## ✅ VALIDATION COMPLETE

This V27.1 prompt includes:
- ✅ Together AI integration (Layer 1 provider)
- ✅ Updated three-layer fallback architecture
- ✅ Mission 2 family focus fix (mother OR father)
- ✅ JSON response format enforcement
- ✅ Testing scripts and monitoring guidelines
- ✅ Complete migration instructions
- ✅ Production deployment checklist

**Use this prompt for all future week generation to ensure**:
1. No rate limiting issues (60 req/min capacity)
2. Proper family topic coverage in Week 2
3. Consistent JSON responses from AI providers
4. Automatic fallback resilience

---

**End of ENGQUEST MASTER PROMPT V27.1**
