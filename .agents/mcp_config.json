{
  "mcpServers": {
    "model-router": {
      "command": "node",
      "args": ["scripts/mcp_model_router.mjs"],
      "description": "Auto-classifies user prompts into Tier 1-4 and recommends optimal model (Flash / Standard / Claude Sonnet Thinking / Gemini Pro). Agent calls classify_task tool at start of each conversation per AGENTS.md protocol.",
      "env": {}
    }
  }
}
