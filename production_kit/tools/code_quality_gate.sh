#!/bin/bash
# =============================================================================
# CODE_QUALITY_GATE.SH — EngQuest3k Full-App Quality Gate
# =============================================================================
#
# PURPOSE:
#   Catches bugs that data validators (validate_week.js) CANNOT catch because
#   they only check data file structure, not:
#     - React component code patterns
#     - Service registrations (STATIC_STATIONS, STATION_VOICE_KEY)
#     - Week data schema / template integrity (cloned from W16 golden standard)
#     - Cross-file consistency (audio paths match week number, AI Tutor V28 schema)
#
# HOW PRODUCTION WORKS:
#   Clone Week 16 (golden standard) → replace content → validate quality gate
#   Gate checks: (1) code patterns + (2) W16 schema compliance + (3) content consistency
#
# CHECKS (41 total):
#   Code pattern checks (React/service bugs):
#   [1]  Images: all <img src> from data use getImageUrl()
#   [2]  MindMap: station='mindmap_speaking' (not 'read')
#   [3]  CDN_WEEKS includes new week (WARNING – upload audio first)
#   [4]  Images: no leftover download*.png before R2 upload
#   [5]  word_match: pair objects not bare numbers
#   [6]  SingaporeMathDisplay bar_model uses getImageUrl()
#   [7]  No raw src={`/images/...`} template literals in JSX
#   [8]  STATIC_STATIONS includes logic_lab + singapore_math + social_quiz
#   [9]  LogicLab.jsx uses correct station name (not 'read' for old puzzles)
#   [10] All TabbedLogicLab display components use getImageUrl()
#
#   Data schema/template checks (W16 clone issues):
#   [11] Both week_NN/ and weeks_easy/week_NN/ directories exist
#   [12] index.js voiceConfig has all 6 required keys
#   [13] index.js stations has all 14 required W16 keys
#   [14] weekId in index.js matches week N
#   [15] Audio/image paths reference week N (not stale from W16)
#   [16] AI Tutor week_NN_real.js: 3 missions + v28_format_notes + nova + pacing
#
#   Data content checks (schema correctness + actual content counts):
#   [17] dictation.js: exports { sentences: [...] } + >= 5 sentence entries (by { id: count)
#   [18] singapore_math.js: schema correct + >= 3 problems (question_en count)
#   [19] writing.js: schema correct + model_sentence line >= 200 chars
#   [20] word_power.js: 6 items (W16-27) or 8 items (W28+), phrasal verb/idiom
#   [20b] grammar.js: exactly 20 exercises per mode
#   [20c] read.js: bold words W1-15≥10, W16+≥13
#   [20d] dictation.js: sentence count equals read.js sentence count
#   [21] mindmap.js: schema correct + >= 36 audio entries (6 stems × 6 branches, by audio: count)
#   [22] metadata.js: week N has real title (not generic "Week N")
#
#   Production readiness checks (content completeness):
#   [23] daily_watch.js: video IDs are unique (no generic fallback from update_videos.js)
#   [24] video_tasks.json: has queries for week N (video search was run)
#   [25] Image prompt files exist for week N in Production_FINAL/IMAGE PROMPTS/
#   [26] Cover image_url paths in read.js/explore.js resolve to actual files on disk
#
#   Content count / registration checks (catches missing wiring):
#   [27] grammar.js: exactly 20 exercises per mode (question_en: count) — see CHECK 20b
#   [28] gameAdaptation.js: week N imports + weekGamesMap entry (GameHub wiring)
#   [29] StoryMissionTab + FreeTalkTab: import/ternary for week N (AI Tutor wiring)
#   [30] games.js: show_tell + make_sentence + ask_me present, >= 10 answer entries
#
#   W22 additions (cross-contamination bugs):
#   [35] vocab.js: Easy word count equals Advanced
#   [36] read.js: >= 50% of week vocab words present (no previous-week contamination)
#   [37] explore.js: >= 8 **bold** markers + >= 100 words (CLIL article, not grammar drill)
#   [38] read.js: if grammar_focus=past, must have >= 3 -ed verb forms
#
#   W23 additions (FreeTalk/image/R2 bugs):
#   [39] conversation_cards: exchanges array present (not legacy prompt_en/starter format)
#   [40] image_prompts: no barmodel_ entries (bar models are auto-generated by Python script)
#   [41] bar model images: all present on R2 CDN (run upload_week_images_r2.py after generate)
#
#   W27 additions:
#   [44] vocab.js audio field names: correct schema (no audio_def/audio_coll)
#   [45] vocab.js collocation quality: body parts, cross-contamination
#   [46] content_vi: no multi-word **phrase** bold in dictation/shadowing/explore
#
# Usage:
#   bash tools/code_quality_gate.sh <week_number>
#   bash tools/code_quality_gate.sh 17
#
# Exit codes:
#   0 = all checks passed (safe to commit)
#   1 = one or more checks failed (DO NOT commit until fixed)
# =============================================================================

set -euo pipefail

# Colors
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
CYAN='\033[0;36m'
BOLD='\033[1m'
NC='\033[0m'

WEEK="${1:-}"
if [ -z "$WEEK" ]; then
  echo -e "${RED}❌ Usage: bash tools/code_quality_gate.sh <week_number>${NC}"
  echo -e "   Example: bash tools/code_quality_gate.sh 17"
  exit 1
fi

WEEK_INT=$(echo "$WEEK" | sed 's/^0*//')   # strip leading zeros for number checks
WEEK_PAD=$(printf '%02d' "$WEEK_INT")      # zero-padded e.g. 08, 17

ERRORS=0
WARNINGS=0

echo -e "${BLUE}${BOLD}═══════════════════════════════════════════════════════════════${NC}"
echo -e "${BLUE}${BOLD}  CODE QUALITY GATE — Week ${WEEK_PAD} (Full App Coverage)${NC}"
echo -e "${BLUE}${BOLD}═══════════════════════════════════════════════════════════════${NC}"
echo ""
echo -e "${CYAN}Checks 1-10: code patterns  |  11-16: data schema  |  17-22: content counts  |  23-26: production  |  27-30: wiring/registration${NC}"
echo ""

# =============================================================================
# CHECK 1: All <img src={}> in components must use getImageUrl()
# =============================================================================
echo -e "${BOLD}[CHECK 1] Image URLs — All <img src> must use getImageUrl()${NC}"
echo "   Scanning: src/components/**/*.jsx, src/modules/**/*.jsx"

# Strategy: find src={something} that looks like a DATA IMAGE field name
# but is NOT wrapped in getImageUrl() call.
# 
# What to CATCH:  src={item.image_url}  src={word.image}  src={problem.bar_model}
# What to SKIP:   src={imageSrc}         (state var already processed by getImageUrl)
#                 src={currentUser.avatar_url}  (external user avatar)
#                 src={av.url}  src={avatar}    (profile images)
#                 src={video.thumb}             (YouTube URL)
#                 _BAK.jsx / _broken.jsx        (dead/backup files)

RAW_IMG_HITS=$(grep -rn --include="*.jsx" --include="*.tsx" \
  'src={[^g(]' \
  src/components/ src/modules/ 2>/dev/null | \
  grep -v "_BAK\." | \
  grep -v "_broken\." | \
  grep -v "getImageUrl" | \
  grep -v "avatar\|avatarUrl\|avatar_url\|\.thumb\|profilePic\|userPhoto" | \
  grep -v "src={imageSrc}" | \
  grep -v "src={currentAvatar}" | \
  grep -v "src={av\." | \
  grep -v "src={url}" | \
  grep -v 'https://' | \
  grep -iE 'src=\{[a-zA-Z]+\.(image_url|image_path|bar_model|cover_image|img_url|photo_url|image|thumbnail)' \
  || true)

if [ -n "$RAW_IMG_HITS" ]; then
  echo -e "   ${RED}❌ FAIL: Found <img> with raw data image path (missing getImageUrl):${NC}"
  echo "$RAW_IMG_HITS" | while IFS= read -r line; do
    echo -e "      ${RED}$line${NC}"
  done
  echo ""
  echo -e "   ${YELLOW}FIX: Add import + wrap:${NC}"
  echo -e "      import { getImageUrl } from '../../utils/imageUrl';"
  echo -e "      src={getImageUrl(item.image_url)}"
  ERRORS=$((ERRORS + 1))
else
  echo -e "   ${GREEN}✅ PASS: All data image src attributes use getImageUrl()${NC}"
fi
echo ""

# =============================================================================
# CHECK 2: MindMapSpeaking.jsx must use station='mindmap_speaking'
# =============================================================================
echo -e "${BOLD}[CHECK 2] MindMap Station Name — must be 'mindmap_speaking'${NC}"
MINDMAP_FILE="src/modules/production/MindMapSpeaking.jsx"

if [ ! -f "$MINDMAP_FILE" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $MINDMAP_FILE not found${NC}"
  WARNINGS=$((WARNINGS + 1))
else
  # Check for wrong station 'read' in speakText calls
  WRONG_STATION=$(grep -n "'read'" "$MINDMAP_FILE" | grep "speakText" || true)
  if [ -n "$WRONG_STATION" ]; then
    echo -e "   ${RED}❌ FAIL: Found station='read' in MindMapSpeaking.jsx speakText calls:${NC}"
    echo "$WRONG_STATION" | while IFS= read -r line; do
      echo -e "      ${RED}$line${NC}"
    done
    echo -e "   ${YELLOW}FIX: Change 'read' → 'mindmap_speaking' in speakText() calls${NC}"
    ERRORS=$((ERRORS + 1))
  else
    # Also verify mindmap_speaking IS present (not replaced with something else wrong)
    CORRECT_STATION=$(grep -c "'mindmap_speaking'" "$MINDMAP_FILE" || true)
    if [ "$CORRECT_STATION" -gt 0 ]; then
      echo -e "   ${GREEN}✅ PASS: MindMapSpeaking uses 'mindmap_speaking' ($CORRECT_STATION occurrences)${NC}"
    else
      echo -e "   ${YELLOW}⚠️  WARNING: No 'mindmap_speaking' found in $MINDMAP_FILE — verify station name${NC}"
      WARNINGS=$((WARNINGS + 1))
    fi
  fi
fi
echo ""

# =============================================================================
# CHECK 3: CDN_WEEKS — on-demand TTS (W16+) or pre-generated R2 (W1-15)
# =============================================================================
echo -e "${BOLD}[CHECK 3] TTS Architecture — verify correct mode for week $WEEK_INT${NC}"
VOICE_SERVICE="src/services/voiceService.js"

if [ ! -f "$VOICE_SERVICE" ]; then
  echo -e "   ${RED}❌ FAIL: $VOICE_SERVICE not found${NC}"; ERRORS=$((ERRORS+1))
elif [ "$WEEK_INT" -ge 16 ]; then
  # W16+: On-demand TTS via Deepgram Worker — CDN_WEEKS is NOT expected to include this week
  CDN_LINE=$(grep "^const CDN_WEEKS" "$VOICE_SERVICE" | head -1)
  if echo "$CDN_LINE" | grep -qw "$WEEK_INT"; then
    echo -e "   ${YELLOW}⚠️  WARNING: Week $WEEK_INT is in CDN_WEEKS — unexpected for on-demand architecture${NC}"
    echo -e "   ${CYAN}   $CDN_LINE${NC}"
    echo -e "   ${YELLOW}   W16+ should NOT be in CDN_WEEKS (on-demand: Worker handles R2 caching)${NC}"
    WARNINGS=$((WARNINGS+1))
  else
    echo -e "   ${GREEN}✅ PASS: Week $WEEK_INT correctly NOT in CDN_WEEKS (on-demand TTS mode)${NC}"
    echo -e "   ${CYAN}   On-demand: audio_url in data = R2 storage key for Deepgram Worker${NC}"
    echo -e "   ${CYAN}   First playback: Worker generates via Deepgram → cached to R2 automatically${NC}"
    echo -e "   ${CYAN}   $CDN_LINE${NC}"
  fi
else
  # W1-15: Pre-generated audio should be in CDN_WEEKS
  CDN_LINE=$(grep "^const CDN_WEEKS" "$VOICE_SERVICE" | head -1)
  if echo "$CDN_LINE" | grep -qw "$WEEK_INT"; then
    echo -e "   ${GREEN}✅ PASS: Week $WEEK_INT in CDN_WEEKS (pre-generated audio on R2)${NC}"
    echo -e "   ${CYAN}   $CDN_LINE${NC}"
  else
    echo -e "   ${YELLOW}⚠️  WARNING: Week $WEEK_INT NOT in CDN_WEEKS${NC}"
    echo -e "   ${CYAN}   Current: $CDN_LINE${NC}"
    echo -e "   ${YELLOW}   ACTION: Upload audio to R2 first, then add $WEEK_INT to CDN_WEEKS${NC}"
    WARNINGS=$((WARNINGS+1))
  fi
fi
echo ""

# =============================================================================
# CHECK 4: No leftover download*.png in week image folders
# =============================================================================
echo -e "${BOLD}[CHECK 4] Image naming — no leftover 'download*.png' files${NC}"
ADV_FOLDER="public/images/week${WEEK_PAD}"
EASY_FOLDER="public/images/week${WEEK_PAD}_easy"
# Also check underscore format
ADV_FOLDER2="public/images/week_${WEEK_PAD}"
EASY_FOLDER2="public/images/week_${WEEK_PAD}_easy"

DOWNLOAD_FILES=""
for folder in "$ADV_FOLDER" "$EASY_FOLDER" "$ADV_FOLDER2" "$EASY_FOLDER2"; do
  if [ -d "$folder" ]; then
    FOUND=$(find "$folder" -name "download*.png" -o -name "download*.jpg" 2>/dev/null || true)
    if [ -n "$FOUND" ]; then
      DOWNLOAD_FILES="${DOWNLOAD_FILES}${FOUND}"$'\n'
    fi
  fi
done

if [ -n "$DOWNLOAD_FILES" ]; then
  echo -e "   ${RED}❌ FAIL: Found unnamed download files (must be renamed before R2 upload):${NC}"
  echo "$DOWNLOAD_FILES" | while IFS= read -r line; do
    [ -n "$line" ] && echo -e "      ${RED}$line${NC}"
  done
  echo -e "   ${YELLOW}FIX: Run: python3 auto_rename.py $WEEK_INT${NC}"
  ERRORS=$((ERRORS + 1))
else
  # Check folders exist at all
  FOLDER_FOUND=false
  for folder in "$ADV_FOLDER" "$EASY_FOLDER" "$ADV_FOLDER2" "$EASY_FOLDER2"; do
    [ -d "$folder" ] && FOLDER_FOUND=true && break
  done
  if [ "$FOLDER_FOUND" = true ]; then
    echo -e "   ${GREEN}✅ PASS: No download*.png files in week image folders${NC}"
  else
    echo -e "   ${YELLOW}⚠️  SKIP: Image folders not found yet (may not be generated yet)${NC}"
    WARNINGS=$((WARNINGS + 1))
  fi
fi
echo ""

# =============================================================================
# CHECK 5: word_match.js — pairs must be objects, not bare numbers
# =============================================================================
echo -e "${BOLD}[CHECK 5] word_match.js — pairs must be definition objects, not numbers${NC}"
WM_ADV="src/data/weeks/week_${WEEK_PAD}/word_match.js"
WM_EASY="src/data/weeks_easy/week_${WEEK_PAD}/word_match.js"
# Also check without underscore prefix
WM_ADV2="src/data/weeks/week${WEEK_PAD}/word_match.js"
WM_EASY2="src/data/weeks_easy/week${WEEK_PAD}/word_match.js"

WM_FILE=""
for f in "$WM_ADV" "$WM_ADV2"; do
  [ -f "$f" ] && WM_FILE="$f" && break
done

if [ -z "$WM_FILE" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: word_match.js not found yet${NC}"
  WARNINGS=$((WARNINGS + 1))
else
  # Bad pattern: pairs: [1, 2, 3 ...] or pairs: [ 1, (number array)
  BAD_PAIRS=$(grep -n "pairs: *\[ *[0-9]" "$WM_FILE" 2>/dev/null || true)
  if [ -n "$BAD_PAIRS" ]; then
    echo -e "   ${RED}❌ FAIL: word_match.js 'pairs' contains bare numbers (should be objects):${NC}"
    echo "$BAD_PAIRS" | while IFS= read -r line; do
      echo -e "      ${RED}$line${NC}"
    done
    echo -e "   ${YELLOW}FIX: Replace number array with proper pair objects:${NC}"
    echo -e "      { word: 'kick', definition: 'to hit with your foot' }"
    ERRORS=$((ERRORS + 1))
  else
    echo -e "   ${GREEN}✅ PASS: word_match.js pairs are properly structured${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 6: LogicLab bar_model images — must use getImageUrl()
# =============================================================================
echo -e "${BOLD}[CHECK 6] LogicLab / SingaporeMath — bar_model must use getImageUrl()${NC}"
SINGAPORE_FILE="src/components/LogicLab/SingaporeMathDisplay.jsx"

if [ ! -f "$SINGAPORE_FILE" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $SINGAPORE_FILE not found${NC}"
  WARNINGS=$((WARNINGS + 1))
else
  # Must import getImageUrl
  HAS_IMPORT=$(grep -c "getImageUrl" "$SINGAPORE_FILE" || true)
  # Must NOT have raw src={problem.bar_model} without getImageUrl
  RAW_BAR=$(grep -n "src={problem\.bar_model}" "$SINGAPORE_FILE" | grep -v "getImageUrl" || true)

  if [ -n "$RAW_BAR" ]; then
    echo -e "   ${RED}❌ FAIL: bar_model uses raw path (missing getImageUrl):${NC}"
    echo -e "      ${RED}$RAW_BAR${NC}"
    echo -e "   ${YELLOW}FIX: src={getImageUrl(problem.bar_model)}${NC}"
    ERRORS=$((ERRORS + 1))
  elif [ "$HAS_IMPORT" -gt 0 ]; then
    echo -e "   ${GREEN}✅ PASS: SingaporeMathDisplay uses getImageUrl() ($HAS_IMPORT occurrences)${NC}"
  else
    echo -e "   ${YELLOW}⚠️  WARNING: getImageUrl not found in SingaporeMathDisplay.jsx — verify manually${NC}"
    WARNINGS=$((WARNINGS + 1))
  fi
fi
echo ""

# =============================================================================
# CHECK 7: No raw /images/ paths in JSX src attributes
# =============================================================================
echo -e "${BOLD}[CHECK 7] JSX — no raw src={\`/images/...\`} in components${NC}"
RAW_TEMPLATE=$(grep -rn --include="*.jsx" --include="*.tsx" \
  'src={`/images/' \
  src/components/ src/modules/ 2>/dev/null || true)

if [ -n "$RAW_TEMPLATE" ]; then
  echo -e "   ${RED}❌ FAIL: Found raw template literal image paths:${NC}"
  echo "$RAW_TEMPLATE" | while IFS= read -r line; do
    echo -e "      ${RED}$line${NC}"
  done
  echo -e "   ${YELLOW}FIX: Use getImageUrl() instead of template literals for image paths${NC}"
  ERRORS=$((ERRORS + 1))
else
  echo -e "   ${GREEN}✅ PASS: No raw template literal image paths in JSX${NC}"
fi
echo ""

# =============================================================================
# CHECK 8: STATIC_STATIONS in voiceService.js — must include W16+ logic stations
# =============================================================================
echo -e "${BOLD}[CHECK 8] STATIC_STATIONS — must include logic_lab + singapore_math + social_quiz${NC}"
VOICE_SVC="src/services/voiceService.js"
if [ ! -f "$VOICE_SVC" ]; then
  echo -e "   ${RED}❌ FAIL: voiceService.js not found${NC}"; ERRORS=$((ERRORS+1))
else
  STATIC_LINE=$(grep "^const STATIC_STATIONS" "$VOICE_SVC" | head -1)
  MISS_STATIC=""
  for st in logic_lab singapore_math social_quiz; do
    echo "$STATIC_LINE" | grep -q "'$st'" || MISS_STATIC="$MISS_STATIC $st"
  done
  if [ -n "$MISS_STATIC" ]; then
    echo -e "   ${RED}❌ FAIL: Missing from STATIC_STATIONS:${MISS_STATIC}${NC}"
    echo -e "   ${CYAN}   $STATIC_LINE${NC}"
    echo -e "   ${YELLOW}FIX: Add missing stations to STATIC_STATIONS array in voiceService.js${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: STATIC_STATIONS includes all logic_lab sub-stations${NC}"
    echo -e "   ${CYAN}   $STATIC_LINE${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 9: LogicLab.jsx — old puzzle path must NOT use station='read'
# =============================================================================
echo -e "${BOLD}[CHECK 9] LogicLab.jsx — puzzle speakText must use 'logic_lab' not 'read'${NC}"
LOGIC_FILE="src/modules/logic/LogicLab.jsx"
if [ ! -f "$LOGIC_FILE" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  WRONG_LOGIC=$(grep -n "speakText.*p\.question\|speakText.*p\.audio" "$LOGIC_FILE" | grep "'read'" || true)
  if [ -n "$WRONG_LOGIC" ]; then
    echo -e "   ${RED}❌ FAIL: station='read' for puzzle audio (should be 'logic_lab'):${NC}"
    echo "$WRONG_LOGIC" | while IFS= read -r line; do echo -e "      ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: Change 'read' → 'logic_lab' in LogicLab.jsx speakText calls${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: LogicLab.jsx uses correct station for old puzzle audio${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 10: TabbedLogicLab + TabbedReadExplore display components use getImageUrl()
# =============================================================================
echo -e "${BOLD}[CHECK 10] Display components — all image fields must use getImageUrl()${NC}"
DISPLAY_ERRORS=""
for f in \
  "src/components/LogicLab/LogicScienceDisplay.jsx" \
  "src/components/LogicLab/SingaporeMathDisplay.jsx" \
  "src/components/LogicLab/SocialQuizDisplay.jsx" \
  "src/components/ReadExplore/TabbedReadExplore.jsx"; do
  [ ! -f "$f" ] && continue
  RAW=$(grep -n 'src={.*\.\(image_url\|bar_model\|cover_image\)}' "$f" | grep -v "getImageUrl" || true)
  [ -n "$RAW" ] && DISPLAY_ERRORS="${DISPLAY_ERRORS}$(basename $f): $RAW\n"
done
if [ -n "$DISPLAY_ERRORS" ]; then
  echo -e "   ${RED}❌ FAIL: Raw image paths in display components:${NC}"
  echo -e "$DISPLAY_ERRORS" | while IFS= read -r line; do
    [ -n "$line" ] && echo -e "      ${RED}$line${NC}"
  done
  echo -e "   ${YELLOW}FIX: Wrap src with getImageUrl()${NC}"; ERRORS=$((ERRORS+1))
else
  echo -e "   ${GREEN}✅ PASS: All display components use getImageUrl()${NC}"
fi
echo ""

echo -e "${CYAN}${BOLD}────────────────── DATA SCHEMA / TEMPLATE CHECKS ──────────────────${NC}"
echo -e "${CYAN}        Validates Week ${WEEK_PAD} data matches W16 golden standard${NC}"
echo -e "${CYAN}${BOLD}────────────────────────────────────────────────────────────────────${NC}"
echo ""

ADV_DIR="src/data/weeks/week_${WEEK_PAD}"
EASY_DIR="src/data/weeks_easy/week_${WEEK_PAD}"
IDX_ADV="${ADV_DIR}/index.js"
IDX_EASY="${EASY_DIR}/index.js"

# =============================================================================
# CHECK 11: Both data directories must exist
# =============================================================================
echo -e "${BOLD}[CHECK 11] Data directories — both advanced and easy must exist${NC}"
MISS_DIRS=""
[ ! -d "$ADV_DIR"  ] && MISS_DIRS="$MISS_DIRS\n      $ADV_DIR"
[ ! -d "$EASY_DIR" ] && MISS_DIRS="$MISS_DIRS\n      $EASY_DIR"
if [ -n "$MISS_DIRS" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: Directories not yet created:${NC}"
  echo -e "$MISS_DIRS"
  echo -e "   ${CYAN}   → Run after content generation (Step 1 of mass_production_final.sh)${NC}"
  WARNINGS=$((WARNINGS+1))
else
  ADV_COUNT=$(ls "$ADV_DIR"/*.js 2>/dev/null | wc -l | tr -d ' ')
  EASY_COUNT=$(ls "$EASY_DIR"/*.js 2>/dev/null | wc -l | tr -d ' ')
  echo -e "   ${GREEN}✅ PASS: Both exist  (Advanced: $ADV_COUNT .js files, Easy: $EASY_COUNT .js files)${NC}"
fi
echo ""

# =============================================================================
# CHECK 12: index.js voiceConfig must have all 6 required keys
# =============================================================================
echo -e "${BOLD}[CHECK 12] voiceConfig — must have: narration / vocabulary / dictation / shadowing / questions / mindmap${NC}"
if [ ! -f "$IDX_ADV" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $IDX_ADV not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  for mode_dir in "$IDX_ADV" "$IDX_EASY"; do
    [ ! -f "$mode_dir" ] && continue
    LABEL=$([ "$mode_dir" = "$IDX_ADV" ] && echo "Advanced" || echo "Easy")
    MISS_V=""
    for key in narration vocabulary dictation shadowing questions mindmap; do
      grep -q "${key}:" "$mode_dir" || MISS_V="$MISS_V $key"
    done
    if [ -n "$MISS_V" ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): voiceConfig missing keys:${MISS_V}${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS ($LABEL): voiceConfig complete${NC}"
    fi
  done
fi
echo ""

# =============================================================================
# CHECK 13: index.js stations must have all 14 required W16 keys
# =============================================================================
echo -e "${BOLD}[CHECK 13] stations — must have all 14 W16 keys${NC}"
REQUIRED_STATIONS="read_explore new_words word_match grammar word_power ask_ai logic_lab dictation shadowing writing explore mindmap_speaking daily_watch game_hub"
if [ ! -f "$IDX_ADV" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $IDX_ADV not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  MISS_ST=""
  for st in $REQUIRED_STATIONS; do
    grep -q "${st}[: ,]" "$IDX_ADV" || MISS_ST="$MISS_ST $st"
  done
  if [ -n "$MISS_ST" ]; then
    echo -e "   ${RED}❌ FAIL: stations missing keys:${MISS_ST}${NC}"
    echo -e "   ${YELLOW}FIX: Add missing station keys in index.js 'stations' object${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: All 14 station keys present in index.js${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 14: weekId in index.js must match week N
# =============================================================================
echo -e "${BOLD}[CHECK 14] weekId — must equal $WEEK_INT in both index.js files${NC}"
if [ ! -f "$IDX_ADV" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  for mode_dir in "$IDX_ADV" "$IDX_EASY"; do
    [ ! -f "$mode_dir" ] && continue
    LABEL=$([ "$mode_dir" = "$IDX_ADV" ] && echo "Advanced" || echo "Easy")
    WID_LINE=$(grep "weekId:" "$mode_dir" | head -1)
    if echo "$WID_LINE" | grep -qE "weekId: *${WEEK_INT}[^0-9]|weekId: *${WEEK_INT}$"; then
      echo -e "   ${GREEN}✅ PASS ($LABEL): weekId = $WEEK_INT${NC}"
    else
      echo -e "   ${RED}❌ FAIL ($LABEL): weekId mismatch → $WID_LINE${NC}"
      echo -e "   ${YELLOW}FIX: Update 'weekId: $WEEK_INT' in ${mode_dir}${NC}"
      ERRORS=$((ERRORS+1))
    fi
  done
fi
echo ""

# =============================================================================
# CHECK 15: Audio/image paths must reference week N (no stale W16 paths)
# =============================================================================
echo -e "${BOLD}[CHECK 15] Path consistency — audio/image refs must use week $WEEK_PAD${NC}"
if [ "$WEEK_INT" -eq 16 ]; then
  echo -e "   ${CYAN}ℹ️  SKIP: Week 16 is the golden standard — no stale-path check needed${NC}"
elif [ ! -d "$ADV_DIR" ] && [ ! -d "$EASY_DIR" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: Data directories not yet generated${NC}"; WARNINGS=$((WARNINGS+1))
else
  STALE_PATHS=""
  for dir in "$ADV_DIR" "$EASY_DIR"; do
    [ ! -d "$dir" ] && continue
    FOUND=$(grep -rn "/audio/week[0-9]\{1,2\}/\|/images/week[0-9]\{1,2\}/" "$dir" 2>/dev/null | \
      grep -v "/week${WEEK_INT}/\|/week${WEEK_PAD}/" | grep -v "^Binary" || true)
    [ -n "$FOUND" ] && STALE_PATHS="${STALE_PATHS}${FOUND}"$'\n'
  done
  if [ -n "$STALE_PATHS" ]; then
    echo -e "   ${RED}❌ FAIL: Stale paths from wrong week:${NC}"
    echo "$STALE_PATHS" | head -10 | while IFS= read -r line; do
      [ -n "$line" ] && echo -e "      ${RED}$line${NC}"
    done
    echo -e "   ${YELLOW}FIX: Replace all /weekXX/ references with /week${WEEK_PAD}/${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: All audio/image paths reference week $WEEK_PAD${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 16: AI Tutor week_NN_real.js — V28 schema completeness
# =============================================================================
echo -e "${BOLD}[CHECK 16] AI Tutor week_${WEEK_PAD}_real.js — V28 schema (3 missions + v28_format_notes + nova)${NC}"
AI_TUTOR="src/data/weeks/week_${WEEK_PAD}_real.js"
if [ ! -f "$AI_TUTOR" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $AI_TUTOR not yet created${NC}"; WARNINGS=$((WARNINGS+1))
else
  AI_ERRS=""
  MC=$(grep -c "mission_id:" "$AI_TUTOR" 2>/dev/null || echo "0")
  [ "$MC" -ne 3 ] && AI_ERRS="${AI_ERRS}\n   • story_missions: $MC mission_id entries (expected 3)"
  grep -q "v28_format_notes"  "$AI_TUTOR" || AI_ERRS="${AI_ERRS}\n   • Missing: v28_format_notes"
  grep -q "nova_instructions" "$AI_TUTOR" || AI_ERRS="${AI_ERRS}\n   • Missing: nova_instructions"
  grep -q "story_character"   "$AI_TUTOR" || AI_ERRS="${AI_ERRS}\n   • Missing: story_character"
  grep -q "minimum_turns"     "$AI_TUTOR" || AI_ERRS="${AI_ERRS}\n   • Missing: minimum_turns (≥10)"
  grep -q "grammar_examples"  "$AI_TUTOR" || AI_ERRS="${AI_ERRS}\n   • Missing: grammar_examples"
  WN_LINE=$(grep "week_number:" "$AI_TUTOR" 2>/dev/null | head -1 || true)
  echo "$WN_LINE" | grep -qE "week_number: *${WEEK_INT}[^0-9]|week_number: *${WEEK_INT}$" || \
    AI_ERRS="${AI_ERRS}\n   • week_number mismatch or missing: '$WN_LINE' (expected week_number: $WEEK_INT)"
  AI_WID=$(grep -E "weekId:|week_id:" "$AI_TUTOR" 2>/dev/null | head -1 || true)
  echo "$AI_WID" | grep -qE "(weekId|week_id): *${WEEK_INT}[^0-9]|(weekId|week_id): *${WEEK_INT}$" || \
    AI_ERRS="${AI_ERRS}\n   • weekId/week_id mismatch or missing: '$AI_WID' (expected: $WEEK_INT)"

  if [ -n "$AI_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL: AI Tutor schema errors:${NC}"
    echo -e "$AI_ERRS" | while IFS= read -r line; do echo -e "      ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: Match V28 schema from week_16_real.js${NC}"; ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: AI Tutor V28 schema complete (3 missions, nova_instructions, v28_format_notes)${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 17: dictation.js must export { sentences: [...] } not a plain array
# =============================================================================
echo -e "${BOLD}[CHECK 17] dictation.js — must export { sentences: [...] } not a plain array${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  DICT_FILE="${dir}/dictation.js"
  [ ! -f "$DICT_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): $DICT_FILE not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue
  # Bad: export default [  (plain array at top level)
  BAD_DICT=$(grep -n "^export default \[" "$DICT_FILE" || true)
  if [ -n "$BAD_DICT" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): dictation.js exports plain array — must be { sentences: [...] }${NC}"
    echo -e "      ${RED}$BAD_DICT${NC}"
    echo -e "   ${YELLOW}FIX: export default { sentences: [ ...your array... ] };${NC}"
    ERRORS=$((ERRORS+1))
  else
    HAS_SENTENCES=$(grep -c "sentences:" "$DICT_FILE" || true)
    if [ "$HAS_SENTENCES" -gt 0 ]; then
      SENT_COUNT=$(grep -c "  { id:" "$DICT_FILE" || true)
      if [ "$SENT_COUNT" -lt 5 ]; then
        echo -e "   ${RED}❌ FAIL ($LABEL): dictation.js has only $SENT_COUNT sentence(s) — need >= 5${NC}"
        ERRORS=$((ERRORS+1))
      else
        echo -e "   ${GREEN}✅ PASS ($LABEL): dictation.js exports { sentences: [...] } with $SENT_COUNT entries${NC}"
      fi
    else
      echo -e "   ${YELLOW}⚠️  WARNING ($LABEL): 'sentences' key not found in dictation.js — verify schema${NC}"
      WARNINGS=$((WARNINGS+1))
    fi
  fi
done
echo ""

# =============================================================================
# CHECK 18: singapore_math.js — correct answer/bar_model/hint schema
# =============================================================================
echo -e "${BOLD}[CHECK 18] singapore_math.js — answer must be string array, bar_model string, hint_en/hint_vi present${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  SM_FILE="${dir}/singapore_math.js"
  [ ! -f "$SM_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): $SM_FILE not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  SM_ERRS=""
  # Bad: answer: [{ label: or answer: [{ value:  (object array instead of string array)
  BAD_ANSWER=$(grep -n "answer: *\[{" "$SM_FILE" || true)
  [ -n "$BAD_ANSWER" ] && SM_ERRS="${SM_ERRS}\n      answer is an object array (must be string array like [\"5\",\"five\"]): $BAD_ANSWER"

  # Bad: bar_model: { (object instead of string path)
  BAD_BAR=$(grep -n "bar_model: *{" "$SM_FILE" || true)
  [ -n "$BAD_BAR" ] && SM_ERRS="${SM_ERRS}\n      bar_model is an object (must be string path like \"/images/...\"/): $BAD_BAR"

  # Must have hint_en and hint_vi (not hints: [...] array)
  HAS_HINT_EN=$(grep -c "hint_en:" "$SM_FILE" || true)
  HAS_HINT_VI=$(grep -c "hint_vi:" "$SM_FILE" || true)
  BAD_HINTS=$(grep -n "hints: *\[" "$SM_FILE" || true)
  [ -n "$BAD_HINTS" ] && SM_ERRS="${SM_ERRS}\n      hints is an array (must be hint_en/hint_vi strings): $BAD_HINTS"
  [ "$HAS_HINT_EN" -eq 0 ] && SM_ERRS="${SM_ERRS}\n      Missing hint_en fields"
  [ "$HAS_HINT_VI" -eq 0 ] && SM_ERRS="${SM_ERRS}\n      Missing hint_vi fields"

  if [ -n "$SM_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): singapore_math.js schema errors:${NC}"
    echo -e "$SM_ERRS" | while IFS= read -r line; do echo -e "   ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: answer=[\"5\",\"five\"], bar_model=\"/images/...\", hint_en=\"...\", hint_vi=\"...\"${NC}"
    ERRORS=$((ERRORS+1))
  else
    PROB_COUNT=$(grep -c "question_en:" "$SM_FILE" || true)
    if [ "$PROB_COUNT" -lt 3 ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): singapore_math.js has only $PROB_COUNT problem(s) — need >= 3${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS ($LABEL): singapore_math.js schema correct ($PROB_COUNT problems)${NC}"
    fi
  fi
done
echo ""

# =============================================================================
# CHECK 19: writing.js must have model_sentence, prompt_en, prompt_vi
# =============================================================================
echo -e "${BOLD}[CHECK 19] writing.js — must have model_sentence, prompt_en, prompt_vi (no example_en/rubric)${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  WR_FILE="${dir}/writing.js"
  [ ! -f "$WR_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): $WR_FILE not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  WR_ERRS=""
  grep -q "model_sentence:" "$WR_FILE" || WR_ERRS="${WR_ERRS}\n      Missing: model_sentence"
  grep -q "prompt_en:"      "$WR_FILE" || WR_ERRS="${WR_ERRS}\n      Missing: prompt_en"
  grep -q "prompt_vi:"      "$WR_FILE" || WR_ERRS="${WR_ERRS}\n      Missing: prompt_vi"
  BAD_EXAMPLE=$(grep -n "example_en:" "$WR_FILE" || true)
  [ -n "$BAD_EXAMPLE" ] && WR_ERRS="${WR_ERRS}\n      Non-standard field 'example_en' (use model_sentence instead): $BAD_EXAMPLE"
  BAD_RUBRIC=$(grep -n "rubric:" "$WR_FILE" || true)
  [ -n "$BAD_RUBRIC" ] && WR_ERRS="${WR_ERRS}\n      Non-standard field 'rubric' (not in W16 schema): $BAD_RUBRIC"

  if [ -n "$WR_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): writing.js schema errors:${NC}"
    echo -e "$WR_ERRS" | while IFS= read -r line; do echo -e "   ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: Add model_sentence/prompt_en/prompt_vi, remove example_en/rubric${NC}"
    ERRORS=$((ERRORS+1))
  else
    MODEL_LINE=$(grep "model_sentence:" "$WR_FILE" | head -1 || true)
    MODEL_CHARS=$(echo "$MODEL_LINE" | wc -c | tr -d ' ')
    MODEL_TEXT=$(echo "$MODEL_LINE" | sed -E 's/^[[:space:]]*model_sentence:[[:space:]]*"(.*)",[[:space:]]*$/\1/' )
    MODEL_WORDS=$(echo "$MODEL_TEXT" | tr -d '"' | wc -w | tr -d ' ')
    MODEL_SENTENCES=$(echo "$MODEL_TEXT" | grep -o '[.!?]' | wc -l | tr -d ' ')

    if [ "$MODEL_CHARS" -lt 200 ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): writing.js model_sentence too short ($MODEL_CHARS chars on that line) — need >= 200${NC}"
      echo -e "   ${YELLOW}   FIX: Expand model_sentence to >= 25 complete sentences covering week vocabulary${NC}"
      ERRORS=$((ERRORS+1))
    elif [ "$MODEL_WORDS" -lt 45 ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): writing.js model_sentence has only $MODEL_WORDS words — need >= 45${NC}"
      echo -e "   ${YELLOW}   FIX: Add richer model with full ideas (not short fragments)${NC}"
      ERRORS=$((ERRORS+1))
    elif [ "$MODEL_SENTENCES" -lt 6 ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): writing.js model_sentence has only $MODEL_SENTENCES sentence(s) — need >= 6${NC}"
      echo -e "   ${YELLOW}   FIX: Ensure model_sentence includes multiple complete sentences for Write/Video guidance${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS ($LABEL): writing.js schema correct ($MODEL_CHARS chars, $MODEL_WORDS words, $MODEL_SENTENCES sentences)${NC}"
    fi
  fi
done
echo ""

# =============================================================================
# CHECK 19.5: Core station schema must match runtime components
# =============================================================================
echo -e "${BOLD}[CHECK 19.5] Core station schema — read/explore/ask_ai/shadowing/logic_science${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  SCHEMA_ERRS=""

  READ_FILE="${dir}/read.js"
  EXP_FILE="${dir}/explore.js"
  ASK_FILE="${dir}/ask_ai.js"
  SH_FILE="${dir}/shadowing.js"
  LOGIC_FILE="${dir}/logic_science.js"

  if [ -f "$READ_FILE" ]; then
    grep -q "content_en:" "$READ_FILE" || SCHEMA_ERRS="${SCHEMA_ERRS}\n   • read.js missing content_en"
    grep -q "comprehension_questions:" "$READ_FILE" || SCHEMA_ERRS="${SCHEMA_ERRS}\n   • read.js missing comprehension_questions"
  else
    SCHEMA_ERRS="${SCHEMA_ERRS}\n   • Missing file: read.js"
  fi

  if [ -f "$EXP_FILE" ]; then
    grep -q "content_en:" "$EXP_FILE" || SCHEMA_ERRS="${SCHEMA_ERRS}\n   • explore.js missing content_en"
    grep -q "check_questions:" "$EXP_FILE" || SCHEMA_ERRS="${SCHEMA_ERRS}\n   • explore.js missing check_questions"
    grep -qE "question(_en)?:" "$EXP_FILE" || SCHEMA_ERRS="${SCHEMA_ERRS}\n   • explore.js missing question block"
  else
    SCHEMA_ERRS="${SCHEMA_ERRS}\n   • Missing file: explore.js"
  fi

  if [ -f "$ASK_FILE" ]; then
    grep -q "prompts:" "$ASK_FILE" || SCHEMA_ERRS="${SCHEMA_ERRS}\n   • ask_ai.js missing prompts array"
    ASK_COUNT=$(grep -c "context_en:" "$ASK_FILE" || true)
    [ "$ASK_COUNT" -lt 5 ] && SCHEMA_ERRS="${SCHEMA_ERRS}\n   • ask_ai.js has only $ASK_COUNT prompt(s) — need >= 5"

    # Validate correctWord presence (C-19.5a)
    CW_COUNT=$(grep -c "correctWord:" "$ASK_FILE" || true)
    [ "$CW_COUNT" -lt 5 ] && SCHEMA_ERRS="${SCHEMA_ERRS}\n   • ask_ai.js: correctWord count=$CW_COUNT — every prompt needs one"

    # Validate question_word_bank size: W1-27 = 6, W28+ = 4 (C-19.5b)
    # Extract week number from directory path
    WEEK_NUM=$(echo "$dir" | grep -oE 'week_[0-9]+' | grep -oE '[0-9]+' | head -1)
    EXPECTED_BANK_SIZE=""
    [ -n "$WEEK_NUM" ] && [ "$WEEK_NUM" -le 27 ] && EXPECTED_BANK_SIZE=6
    [ -n "$WEEK_NUM" ] && [ "$WEEK_NUM" -ge 28 ] && EXPECTED_BANK_SIZE=4
    if [ -n "$EXPECTED_BANK_SIZE" ]; then
      # Count word_bank arrays that don't have exactly $EXPECTED_BANK_SIZE items
      BANK_SIZE_ERRS=$(awk -v expected="$EXPECTED_BANK_SIZE" '
        /question_word_bank:/ { in_bank=1; count=0 }
        in_bank && /^\s*\[/ { in_array=1 }
        in_bank && in_array {
          gsub(/[",\[\]]/, "")
          gsub(/^\s+/, "")
          if ($0 != "") count++
        }
        in_bank && in_array && /\]/ {
          if (count != expected) {
            printf "   • word_bank has %d items (expected %d)\n", count, expected > "/dev/stderr"
          }
          in_bank=0; in_array=0
        }
      ' "$ASK_FILE" 2>&1 || true)
      [ -n "$BANK_SIZE_ERRS" ] && SCHEMA_ERRS="${SCHEMA_ERRS}\n${BANK_SIZE_ERRS}"
    fi

    # Validate question_frame starts with ___ (C-19.5c)
    FRAME_ERR=$(awk -F'"' '
      /question_frame:/ {
        frame_val = $2
        gsub(/^[ \t]+/, "", frame_val)
        if (substr(frame_val, 1, 3) != "___") {
          printf "   • question_frame does not start with ___: %s\n", frame_val > "/dev/stderr"
        }
      }
    ' "$ASK_FILE" 2>&1 || true)
    [ -n "$FRAME_ERR" ] && SCHEMA_ERRS="${SCHEMA_ERRS}\n${FRAME_ERR}"
  else
    SCHEMA_ERRS="${SCHEMA_ERRS}\n   • Missing file: ask_ai.js"
  fi

  if [ -f "$SH_FILE" ]; then
    grep -q "script:\|sentences:" "$SH_FILE" || SCHEMA_ERRS="${SCHEMA_ERRS}\n   • shadowing.js missing script/sentences array"
    SH_COUNT=$(grep -c "text:" "$SH_FILE" || true)
    [ "$SH_COUNT" -lt 8 ] && SCHEMA_ERRS="${SCHEMA_ERRS}\n   • shadowing.js has only $SH_COUNT line(s) — need >= 8"
  else
    SCHEMA_ERRS="${SCHEMA_ERRS}\n   • Missing file: shadowing.js"
  fi

  if [ -f "$LOGIC_FILE" ]; then
    grep -q "questions:" "$LOGIC_FILE" || SCHEMA_ERRS="${SCHEMA_ERRS}\n   • logic_science.js missing questions array"
    LOGIC_COUNT=$(grep -c "question_en:" "$LOGIC_FILE" || true)
    [ "$LOGIC_COUNT" -lt 3 ] && SCHEMA_ERRS="${SCHEMA_ERRS}\n   • logic_science.js has only $LOGIC_COUNT question(s) — need >= 3"
  else
    SCHEMA_ERRS="${SCHEMA_ERRS}\n   • Missing file: logic_science.js"
  fi

  if [ -n "$SCHEMA_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): Core schema mismatch:${NC}"
    echo -e "$SCHEMA_ERRS" | while IFS= read -r line; do echo -e "   ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: Align files to W16 runtime schema to avoid endless loading states${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): core runtime schema is valid${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 20: word_power.js — 6 items (W16-27) or 8 items (W28+), each must be a phrasal verb/idiom
# =============================================================================
echo -e "${BOLD}[CHECK 20] word_power.js — 6 items (W16-27) or 8 items (W28+), phrasal verb/idiom structure${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  WP_FILE="${dir}/word_power.js"
  [ ! -f "$WP_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): $WP_FILE not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  WORD_COUNT=$(grep -cE "^\s*id:" "$WP_FILE" || true)

  if [ "$WEEK_INT" -ge 28 ]; then
    # W28+: 8 items required
    if [ "$WORD_COUNT" -ne 8 ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): word_power.js has $WORD_COUNT items — W28+ standard requires exactly 8${NC}"
      echo -e "   ${YELLOW}FIX: Keep exactly 8 phrasal verb/idiom items${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS ($LABEL): word_power.js has exactly 8 entries${NC}"
    fi
  elif [ "$WEEK_INT" -ge 16 ]; then
    # W16-27: 6 items required
    if [ "$WORD_COUNT" -ne 6 ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): word_power.js has $WORD_COUNT items — W16-27 standard requires exactly 6${NC}"
      echo -e "   ${YELLOW}FIX: Keep exactly 6 phrasal verb/idiom items${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS ($LABEL): word_power.js has exactly 6 entries${NC}"
    fi
  else
    # W1-15: at least 6
    if [ "$WORD_COUNT" -lt 6 ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): word_power.js has only $WORD_COUNT item(s) — need at least 6${NC}"
      echo -e "   ${YELLOW}FIX: Add more items until count >= 6${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS ($LABEL): word_power.js has $WORD_COUNT items (>= 6)${NC}"
    fi
  fi

  # Validate each `word:` field is a multi-word phrase (must contain a space)
  SINGLE_WORD_LINES=$(grep -E "^\s+word: \"[^ \"]+\"," "$WP_FILE" | grep -v "audio_word\|image_url\|audio_" || true)
  if [ -n "$SINGLE_WORD_LINES" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): word_power.js has single-word entries (should be phrasal verbs/idioms like 'run a race', 'take a nap')${NC}"
    echo "$SINGLE_WORD_LINES" | while read line; do
      echo -e "   ${YELLOW}   → $line${NC}"
    done
    echo -e "   ${YELLOW}FIX: word field must be a multi-word phrase (2+ words)${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): all word entries are multi-word phrases${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 20b: grammar.js — exactly 20 exercises per mode (BUG-W28-A)
# =============================================================================
echo -e "${BOLD}[CHECK 20b] grammar.js — exactly 20 exercises per mode${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  GRAM_FILE="${dir}/grammar.js"
  [ ! -f "$GRAM_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): $GRAM_FILE not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  EX_COUNT=$(node -e "
    try {
      const m = require('./$GRAM_FILE');
      const arr = m.default.exercises || m.default.grammar || m.default.questions || [];
      console.log(arr.length);
    } catch(e) { console.log(0); }
  " 2>/dev/null || echo "0")
  if [ "$EX_COUNT" -ne 20 ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): grammar.js has $EX_COUNT exercises — must be exactly 20${NC}"
    echo -e "   ${YELLOW}FIX: grammar.js must have exactly 20 exercises (both Advanced and Easy)${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): grammar.js has exactly 20 exercises${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 20c: read.js — bold ONLY chunks (collocations), no single-word bolds (BUG-B12, Fundamental Principle)
# =============================================================================
echo -e "${BOLD}[CHECK 20c] read.js — bold CHUNKS only: single-word bold=0, chunk bold≥10(W1-15) or ≥13(W16+)${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  READ_FILE="${dir}/read.js"
  [ ! -f "$READ_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): $READ_FILE not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  if [ "$WEEK_INT" -ge 16 ]; then
    MIN_CHUNK=13
  else
    MIN_CHUNK=10
  fi

  # Separate single-word bolds from chunk bolds
  SINGLE_WORDS=$(grep -o '\*\*[^*\n]*\*\*' "$READ_FILE" 2>/dev/null | sed 's/\*\*//g' | grep -v " " || true)
  CHUNK_COUNT=$(grep -o '\*\*[^*]*\*\*' "$READ_FILE" 2>/dev/null | sed 's/\*\*//g' | grep " " | wc -l | tr -d '[:space:]' || echo 0)
  SINGLE_COUNT=$(echo "$SINGLE_WORDS" | grep -c . || echo 0)
  SINGLE_EXAMPLES=$(echo "$SINGLE_WORDS" | head -3 | tr '\n' ' ')

  if [ "$SINGLE_COUNT" -gt 0 ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): $SINGLE_COUNT single-word bold(s) found — must be 0"
    echo -e "      Single-word bolds: $SINGLE_EXAMPLES"
    echo -e "      FIX: Bold the FULL chunk instead (e.g. **ran down** not **ran**)${NC}"
    ERRORS=$((ERRORS+1))
  fi

  if [ "$CHUNK_COUNT" -lt "$MIN_CHUNK" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): only $CHUNK_COUNT bold chunk(s) — need ≥$MIN_CHUNK"
    echo -e "      FIX: Bold at least $MIN_CHUNK chunk(s)/collocation(s) in content_en${NC}"
    ERRORS=$((ERRORS+1))
  fi

  if [ "$SINGLE_COUNT" -eq 0 ] && [ "$CHUNK_COUNT" -ge "$MIN_CHUNK" ]; then
    echo -e "   ${GREEN}✅ PASS ($LABEL): $CHUNK_COUNT bold chunks, 0 single-word bolds${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 20d: dictation.js sentence count must equal read.js sentence count (BUG-W28-C)
# =============================================================================
echo -e "${BOLD}[CHECK 20d] dictation.js — sentence count must match read.js sentence count${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  READ_FILE="${dir}/read.js"
  DICT_FILE="${dir}/dictation.js"
  [ ! -f "$READ_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): read.js not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue
  [ ! -f "$DICT_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): dictation.js not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  # Count sentences in read.js content_en (split by period — this matches dictation.js splitting)
  READ_SENTS=$(node -e "const m=require('./${dir}/read.js'); console.log((m.default.content_en||'').split('.').filter(s=>s.trim().length>0).length)" 2>/dev/null || echo "0")

  # Count audio_url entries in dictation.js
  DICT_COUNT=$(node -e "const m=require('./${dir}/dictation.js'); console.log((m.default.sentences||[]).length)" 2>/dev/null || echo "0")

  if [ "$READ_SENTS" -ne "$DICT_COUNT" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): read.js has $READ_SENTS sentences but dictation.js has $DICT_COUNT items${NC}"
    echo -e "   ${YELLOW}FIX: dictation.js sentences MUST exactly match read.js sentences (copied, not recreated)${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): dictation.js ($DICT_COUNT) matches read.js ($READ_SENTS sentences)${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 21: mindmap.js — branchLabels keyed by stem text, no completions in centerStems
# =============================================================================
echo -e "${BOLD}[CHECK 21] mindmap.js — branchLabels must be keyed by stem text, no completions array${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  MM_FILE="${dir}/mindmap.js"
  [ ! -f "$MM_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): $MM_FILE not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  MM_ERRS=""
  # Bad: completions: [ in centerStems
  BAD_COMPLETIONS=$(grep -n "completions:" "$MM_FILE" || true)
  [ -n "$BAD_COMPLETIONS" ] && MM_ERRS="${MM_ERRS}\n      Found 'completions:' — must use branchLabels[stemText] instead: $BAD_COMPLETIONS"

  # Bad: branchLabels with simple string values (not arrays of {text,audio})
  # Detect: branchLabels: { weather: "Weather or weather: { (category object keys)
  BAD_BL_STRING=$(grep -n "branchLabels:" "$MM_FILE" | head -1 || true)
  # Check if branchLabels uses category name keys (not stem sentence keys)
  BAD_CATEGORY=$(grep -n "weather:\|clothing:\|science:\|action:" "$MM_FILE" | grep -v "#\|//" || true)
  [ -n "$BAD_CATEGORY" ] && MM_ERRS="${MM_ERRS}\n      branchLabels uses category name keys (must be keyed by exact stem text): $BAD_CATEGORY"

  if [ -n "$MM_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): mindmap.js schema errors:${NC}"
    echo -e "$MM_ERRS" | while IFS= read -r line; do echo -e "   ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: branchLabels[\"exact stem text\"] = [{text,audio},...] — no completions${NC}"
    ERRORS=$((ERRORS+1))
  else
    BRANCH_COUNT=$(grep -c '"audio":' "$MM_FILE" || true)
    if [ "$BRANCH_COUNT" -lt 36 ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): mindmap.js has only $BRANCH_COUNT audio entries — need >= 36 (6 stems × 6 branches)${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS ($LABEL): mindmap.js schema correct ($BRANCH_COUNT audio entries)${NC}"
    fi
  fi
done
echo ""

# =============================================================================
# CHECK 22: metadata.js — week N must not have generic "Week N" title
# =============================================================================
echo -e "${BOLD}[CHECK 22] metadata.js — week $WEEK_INT must have a real title (not generic 'Week $WEEK_INT')${NC}"
META_FILE="src/data/weeks/metadata.js"
if [ ! -f "$META_FILE" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $META_FILE not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  # Look for the week entry and check for generic title
  GENERIC_TITLE=$(grep -A2 "^  ${WEEK_INT}:" "$META_FILE" | grep -E "title_en: \"Week ${WEEK_INT}\"|title_en: 'Week ${WEEK_INT}'" || true)
  if [ -n "$GENERIC_TITLE" ]; then
    echo -e "   ${RED}❌ FAIL: metadata.js week $WEEK_INT still has generic title 'Week $WEEK_INT'${NC}"
    echo -e "      ${RED}$GENERIC_TITLE${NC}"
    echo -e "   ${YELLOW}FIX: Update title_en and title_vi in metadata.js for week $WEEK_INT${NC}"
    ERRORS=$((ERRORS+1))
  else
    ACTUAL_TITLE=$(grep -A2 "^  ${WEEK_INT}:" "$META_FILE" | grep "title_en:" | head -1 || true)
    if [ -n "$ACTUAL_TITLE" ]; then
      echo -e "   ${GREEN}✅ PASS: metadata.js week $WEEK_INT has real title:${NC}"
      echo -e "   ${CYAN}   $ACTUAL_TITLE${NC}"
    else
      echo -e "   ${YELLOW}⚠️  WARNING: Could not verify week $WEEK_INT title in metadata.js${NC}"
      WARNINGS=$((WARNINGS+1))
    fi
  fi
fi
echo ""

# =============================================================================
# CHECK 23: daily_watch.js — must have unique video IDs (no generic fallback)
# =============================================================================
echo -e "${BOLD}[CHECK 23] daily_watch.js — video IDs must be unique (no generic fallback)${NC}"
DW_ADV="src/data/weeks/week_${WEEK_PAD}/daily_watch.js"
DW_EASY="src/data/weeks_easy/week_${WEEK_PAD}/daily_watch.js"

for dw_file in "$DW_ADV" "$DW_EASY"; do
  [ ! -f "$dw_file" ] && continue
  LABEL=$([ "$dw_file" = "$DW_ADV" ] && echo "Advanced" || echo "Easy")

  VIDEO_IDS=$(grep -o 'videoId: *"[^"]*"' "$dw_file" | grep -o '"[^"]*"$' | tr -d '"' | sort)
  UNIQUE_COUNT=$(echo "$VIDEO_IDS" | sort -u | grep -c . || true)
  TOTAL_COUNT=$(echo "$VIDEO_IDS" | grep -c . || true)

  if [ "$TOTAL_COUNT" -eq 0 ]; then
    echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): No videoId fields found in $dw_file${NC}"
    WARNINGS=$((WARNINGS+1))
  elif [ "$UNIQUE_COUNT" -lt 2 ]; then
    FALLBACK_ID=$(echo "$VIDEO_IDS" | head -1)
    echo -e "   ${RED}❌ FAIL ($LABEL): All $TOTAL_COUNT videos share same ID '$FALLBACK_ID' — generic fallback!${NC}"
    echo -e "   ${YELLOW}FIX: Restore real topic-specific video IDs in $dw_file${NC}"
    ERRORS=$((ERRORS+1))
  else
    BAD_FORMAT=$(echo "$VIDEO_IDS" | grep -vE '^[A-Za-z0-9_-]{11}$' || true)
    if [ -n "$BAD_FORMAT" ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL): Invalid videoId format detected:${NC}"
      echo "$BAD_FORMAT" | while IFS= read -r vid; do echo -e "   ${RED}   • $vid${NC}"; done
      echo -e "   ${YELLOW}FIX: videoId must be valid 11-char YouTube ID${NC}"
      ERRORS=$((ERRORS+1))
      continue
    fi

    THUMB_FAIL=0
    if command -v curl >/dev/null 2>&1; then
      while IFS= read -r vid; do
        [ -z "$vid" ] && continue
        CODE=$(curl -s -o /dev/null -w "%{http_code}" "https://img.youtube.com/vi/${vid}/mqdefault.jpg" || echo "000")
        if [ "$CODE" = "404" ] || [ "$CODE" = "000" ]; then
          echo -e "   ${RED}❌ FAIL ($LABEL): Thumbnail check failed for videoId '${vid}' (HTTP ${CODE})${NC}"
          THUMB_FAIL=1
        fi
      done <<EOF
$VIDEO_IDS
EOF
    else
      echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): curl not available, thumbnail validation skipped${NC}"
      WARNINGS=$((WARNINGS+1))
    fi

    if [ "$THUMB_FAIL" -eq 1 ]; then
      echo -e "   ${YELLOW}FIX: Re-run update_videos.js or replace dead YouTube IDs${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS ($LABEL): $TOTAL_COUNT videos, $UNIQUE_COUNT unique IDs + thumbnails reachable${NC}"
    fi
  fi
done
echo ""

# =============================================================================
# CHECK 23b: Image paths must NOT reference any previous week folder
# =============================================================================
echo -e "${BOLD}[CHECK 23b] Image paths - must use /images/week${WEEK_PAD}/ only${NC}"
CROSS_WEEK_ERRORS=0
for js_file in \
  "src/data/weeks/week_${WEEK_PAD}/vocab.js" \
  "src/data/weeks/week_${WEEK_PAD}/word_power.js" \
  "src/data/weeks/week_${WEEK_PAD}/explore.js" \
  "src/data/weeks/week_${WEEK_PAD}/read.js" \
  "src/data/weeks_easy/week_${WEEK_PAD}/vocab.js" \
  "src/data/weeks_easy/week_${WEEK_PAD}/word_power.js" \
  "src/data/weeks_easy/week_${WEEK_PAD}/explore.js" \
  "src/data/weeks_easy/week_${WEEK_PAD}/read.js"; do
  [ ! -f "$js_file" ] && continue
  BAD_PATHS=$(grep "image_url" "$js_file" | grep "/images/week" | grep -v "/images/week${WEEK_INT}/" | grep -v "/images/week${WEEK_INT}_easy/" | grep -v "/images/week${WEEK_PAD}/" | grep -v "/images/week${WEEK_PAD}_easy/" || true)
  if [ -n "$BAD_PATHS" ]; then
    echo -e "   ${RED}FAIL: $js_file uses image path from wrong week:${NC}"
    while IFS= read -r line; do echo -e "   ${RED}   -> $line${NC}"; done <<< "$BAD_PATHS"
    echo -e "   ${YELLOW}FIX: Use /images/week${WEEK_PAD}/<name>.jpg from image_prompts.txt${NC}"
    CROSS_WEEK_ERRORS=$((CROSS_WEEK_ERRORS+1))
  fi
done
if [ "$CROSS_WEEK_ERRORS" -eq 0 ]; then
  echo -e "   ${GREEN}PASS: All image_url paths use correct /images/week${WEEK_PAD}/ folder${NC}"
else
  ERRORS=$((ERRORS+CROSS_WEEK_ERRORS))
fi
echo ""

# =============================================================================
# CHECK 23c: image_url filenames must match entries in image_prompts.txt
# =============================================================================
echo -e "${BOLD}[CHECK 23c] Image filenames must match image_prompts.txt${NC}"
PROMPTS_FILE_23C="Production_FINAL/IMAGE PROMPTS/week_${WEEK_PAD}_image_prompts.txt"
if [ ! -f "$PROMPTS_FILE_23C" ]; then
  echo -e "   ${YELLOW}SKIP: $PROMPTS_FILE_23C not found${NC}"
  WARNINGS=$((WARNINGS+1))
else
  MISMATCH_23C=0
  for js_file in \
    "src/data/weeks/week_${WEEK_PAD}/vocab.js" \
    "src/data/weeks/week_${WEEK_PAD}/word_power.js" \
    "src/data/weeks/week_${WEEK_PAD}/explore.js" \
    "src/data/weeks_easy/week_${WEEK_PAD}/vocab.js" \
    "src/data/weeks_easy/week_${WEEK_PAD}/word_power.js" \
    "src/data/weeks_easy/week_${WEEK_PAD}/explore.js"; do
    [ ! -f "$js_file" ] && continue
    USED_FNAMES=$(grep "image_url" "$js_file" \
      | grep -oE "/images/week[0-9]+/[a-z0-9_]+[.]jpg" \
      | sed "s|/images/week${WEEK_PAD}/||" \
      | grep -v "^barmodel" || true)
    while IFS= read -r fname; do
      [ -z "$fname" ] && continue
      if ! grep -qF "Filename: $fname" "$PROMPTS_FILE_23C"; then
        echo -e "   ${RED}FAIL ($js_file): image $fname not declared in image_prompts.txt${NC}"
        MISMATCH_23C=1
      fi
    done <<< "$USED_FNAMES"
  done
  if [ "$MISMATCH_23C" -eq 0 ]; then
    echo -e "   ${GREEN}PASS: All image_url filenames declared in image_prompts.txt${NC}"
  else
    echo -e "   ${YELLOW}FIX: Update image_url to exact filenames in image_prompts.txt${NC}"
    ERRORS=$((ERRORS+1))
  fi
fi
echo ""


# =============================================================================
# CHECK 24: video_tasks.json — must have week N queries
# =============================================================================
echo -e "${BOLD}[CHECK 24] video_tasks.json — must include Week ${WEEK_PAD} queries${NC}"
VT_FILE="src/data/video_tasks.json"
if [ ! -f "$VT_FILE" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: video_tasks.json not found${NC}"
  WARNINGS=$((WARNINGS+1))
else
  HAS_WEEK=$(grep -c "\"weekId\": *${WEEK_INT}[^0-9]\|\"weekId\": *${WEEK_INT}$\|week_${WEEK_PAD}" "$VT_FILE" 2>/dev/null || true)
  if [ "$HAS_WEEK" -eq 0 ]; then
    echo -e "   ${RED}❌ FAIL: video_tasks.json missing Week ${WEEK_PAD} entries${NC}"
    echo -e "   ${YELLOW}FIX: Add week_${WEEK_PAD} and week_${WEEK_PAD}_easy queries to video_tasks.json${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: video_tasks.json has Week ${WEEK_PAD} entries ($HAS_WEEK occurrences)${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 24b: Bar model paths + files must be valid
# Naming: barmodel_w{NN}_{mode}_p{n}_v{n}.jpg | Files must exist, min 1KB
# =============================================================================
echo -e "${BOLD}[CHECK 24b] Bar models — path naming + file existence${NC}"
BAR_RESULT=$(node tools/validate_barmodels.js $WEEK_INT 2>&1)
BAR_EXIT=$?
if [ $BAR_EXIT -eq 2 ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: no singapore_math.js for Week ${WEEK_PAD}${NC}"
  WARNINGS=$((WARNINGS+1))
elif [ $BAR_EXIT -ne 0 ]; then
  echo "$BAR_RESULT" | sed 's/^/   /'
  echo -e "   ${RED}❌ FAIL: bar model paths or files invalid${NC}"
  ERRORS=$((ERRORS+1))
else
  echo -e "   ${GREEN}✅ PASS: All bar model paths and files valid${NC}"
fi
echo ""

# =============================================================================
# CHECK 24c: Daily watch video thumbnails must be accessible (200 OK)
# All videoIds must return 200 for YouTube mqdefault.jpg thumbnail
# CRITICAL: Never use placeholder/fake video IDs — run update_videos.js
# =============================================================================
echo -e "${BOLD}[CHECK 24c] Daily Watch — video thumbnail accessibility${NC}"
VID_RESULT=$(node tools/validate_video_thumbnails.js $WEEK_INT 2>&1)
VID_EXIT=$?
if [ $VID_EXIT -eq 2 ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: no daily_watch.js for Week ${WEEK_PAD}${NC}"
  WARNINGS=$((WARNINGS+1))
elif [ $VID_EXIT -ne 0 ]; then
  echo "$VID_RESULT" | sed 's/^/   /'
  echo -e "   ${RED}❌ FAIL: video thumbnails not accessible${NC}"
  echo -e "   ${YELLOW}FIX: Run: node tools/update_videos.js ${WEEK_INT}${NC}"
  ERRORS=$((ERRORS+1))
else
  echo -e "   ${GREEN}✅ PASS: All video thumbnails accessible (200 OK)${NC}"
fi
echo ""

# =============================================================================
# CHECK 24d: Station wiring — daily_watch must be imported AND registered in index.js
# Symptom: station file exists but not imported -> undefined -> "Loading..." forever
# =============================================================================
echo -e "${BOLD}[CHECK 24d] Station wiring — daily_watch in index.js${NC}"
for mode in adv easy; do
  SUFFIX=""
  if [ "$mode" = "easy" ]; then SUFFIX="_easy"; fi
  INDEX_FILE="src/data/weeks${SUFFIX}/week_${WEEK_PAD}/index.js"
  STATION_FILE="src/data/weeks${SUFFIX}/week_${WEEK_PAD}/daily_watch.js"
  MODE_LABEL=$(echo "$mode" | tr '[:lower:]' '[:upper:]')

  if [ ! -f "$INDEX_FILE" ]; then
    echo -e "   ${YELLOW}⚠️  SKIP [$MODE_LABEL]: index.js not found${NC}"
    continue
  fi
  if [ ! -f "$STATION_FILE" ]; then
    echo -e "   ${YELLOW}⚠️  SKIP [$MODE_LABEL]: no daily_watch.js file${NC}"
    continue
  fi

  IMPORT_COUNT=$(grep -c "import daily_watch" "$INDEX_FILE" 2>/dev/null || echo 0)
  STATION_COUNT=$(grep -cE "daily_watch[,:]" "$INDEX_FILE" 2>/dev/null || echo 0)

  if [ "$IMPORT_COUNT" -ge 1 ] && [ "$STATION_COUNT" -ge 1 ]; then
    echo -e "   ${GREEN}✅ [$MODE_LABEL] daily_watch: imported + stations wired${NC}"
  elif [ "$IMPORT_COUNT" -ge 1 ]; then
    echo -e "   ${RED}❌ [$MODE_LABEL] daily_watch: imported but NOT in stations{} object${NC}"
    echo -e "   ${YELLOW}   FIX: Add daily_watch: daily_watch to stations{} in index.js${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${RED}❌ [$MODE_LABEL] daily_watch: NOT imported in index.js${NC}"
    echo -e "   ${YELLOW}   FIX: Add import + stations{} entry for daily_watch in index.js${NC}"
    ERRORS=$((ERRORS+1))
  fi
done
echo ""

# =============================================================================
# CHECK 24e: singapore_math.js must export 'problems' not 'questions'
# TabbedLogicLab reads weekData.singapore_math.problems — using questions[] = no bar model
# =============================================================================
echo -e "${BOLD}[CHECK 24e] Singapore Math — 'problems' field (not 'questions')${NC}"
for mode in adv easy; do
  SUFFIX=""
  if [ "$mode" = "easy" ]; then SUFFIX="_easy"; fi
  SM_FILE="src/data/weeks${SUFFIX}/week_${WEEK_PAD}/singapore_math.js"
  MODE_LABEL=$(echo "$mode" | tr '[:lower:]' '[:upper:]')
  if [ ! -f "$SM_FILE" ]; then
    echo -e "   ${YELLOW}⚠️  SKIP [$MODE_LABEL]: no singapore_math.js${NC}"
    continue
  fi
  if grep -q "^  questions:" "$SM_FILE" 2>/dev/null; then
    echo -e "   ${RED}❌ [\$MODE_LABEL] WRONG: uses 'questions' — must be 'problems'${NC}"
    ERRORS=$((ERRORS+1))
  elif grep -q "^  problems:" "$SM_FILE" 2>/dev/null; then
    echo -e "   ${GREEN}✅ [\$MODE_LABEL] uses 'problems' field${NC}"
  else
    echo -e "   ${YELLOW}⚠️  [\$MODE_LABEL] could not detect field${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 25: Image prompt files must exist for week N
# Week 20+: one unified prompt file for non-bar-model assets.
# Week < 20: legacy mode (advanced + optional easy/shared prompts).
# =============================================================================
echo -e "${BOLD}[CHECK 25] Image prompts - files must exist for Week ${WEEK_PAD}${NC}"
IMG_PROMPTS_DIR="Production_FINAL/IMAGE PROMPTS"
ADV_PROMPTS="${IMG_PROMPTS_DIR}/week_${WEEK_PAD}_image_prompts.txt"
EASY_PROMPTS="${IMG_PROMPTS_DIR}/week_${WEEK_PAD}_easy_image_prompts.txt"

if [ "$WEEK_INT" -ge 20 ]; then
  if [ ! -f "$ADV_PROMPTS" ]; then
    echo -e "   ${RED}FAIL: Missing unified image prompts file: $ADV_PROMPTS${NC}"
    echo -e "   ${YELLOW}FIX: Create ONE file week_${WEEK_PAD}_image_prompts.txt for vocab/wordpower/covers assets${NC}"
    ERRORS=$((ERRORS+1))
  else
    PROMPT_COUNT=$(grep -c "Filename:" "$ADV_PROMPTS" || true)
    if [ "$PROMPT_COUNT" -lt 21 ]; then
      echo -e "   ${RED}FAIL: $ADV_PROMPTS has only $PROMPT_COUNT prompts - need >= 21 for W20+${NC}"
      echo -e "   ${YELLOW}FIX: Include non-bar-model assets (vocab/wordpower/covers). Bar models are handled separately.${NC}"
      ERRORS=$((ERRORS+1))
    else
        echo -e "   ${GREEN}PASS: Unified prompts found ($PROMPT_COUNT lines)${NC}"
    fi
  fi

  echo -e "   ${GREEN}PASS (Easy): Week ${WEEK_PAD} uses unified prompts file (no separate easy prompt file required)${NC}"
  echo ""
else
  if [ ! -f "$ADV_PROMPTS" ]; then
    echo -e "   ${RED}FAIL: Missing Advanced image prompts: $ADV_PROMPTS${NC}"
    echo -e "   ${YELLOW}FIX: Create $ADV_PROMPTS with 26 image prompts (vocab/wordpower/covers/barmodels)${NC}"
    ERRORS=$((ERRORS+1))
  else
    PROMPT_COUNT=$(grep -c "Filename:" "$ADV_PROMPTS" || true)
    if [ "$PROMPT_COUNT" -ne 26 ]; then
      echo -e "   ${RED}FAIL (Advanced): $ADV_PROMPTS has $PROMPT_COUNT prompts - need exactly 26${NC}"
      echo -e "   ${YELLOW}FIX: 13 vocab + 6 wordpower (collocations) + 5 barmodels + 2 covers = 26${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}PASS (Advanced): Found 26 image prompts${NC}"
    fi
  fi

  EASY_SHARED=false
  if [ -f "$EASY_DIR/vocab.js" ]; then
    EASY_IMG_REFS=$(grep "image_url" "$EASY_DIR/vocab.js" 2>/dev/null | grep -c "week${WEEK}_easy" || true)
    [ "${EASY_IMG_REFS:-0}" -eq 0 ] && EASY_SHARED=true
  fi

  if [ "$EASY_SHARED" = true ]; then
    echo -e "   ${GREEN}PASS (Easy): Shared images with Advanced - no separate prompts file needed${NC}"
  elif [ ! -f "$EASY_PROMPTS" ]; then
    echo -e "   ${RED}FAIL: Missing Easy image prompts: $EASY_PROMPTS${NC}"
    echo -e "   ${YELLOW}FIX: Either create $EASY_PROMPTS OR point all Easy image_url to /images/week${WEEK}/ (shared mode)${NC}"
    ERRORS=$((ERRORS+1))
  else
    PROMPT_COUNT=$(grep -c "Filename:" "$EASY_PROMPTS" || true)
    echo -e "   ${GREEN}PASS (Easy): Found $PROMPT_COUNT image prompts${NC}"
  fi
  echo ""
fi
# =============================================================================
# CHECK 26: Cover image_url paths in read.js/explore.js must exist on disk
# =============================================================================
echo -e "${BOLD}[CHECK 26] Cover images — image_url in read.js/explore.js must match actual files on disk${NC}"
if [ ! -d "$ADV_DIR" ] && [ ! -d "$EASY_DIR" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: Data directories not yet generated${NC}"; WARNINGS=$((WARNINGS+1))
else
  COVER_ERRORS=0
  for JS_FILE in \
    "$ADV_DIR/read.js" \
    "$ADV_DIR/explore.js" \
    "$EASY_DIR/read.js" \
    "$EASY_DIR/explore.js"; do
    [ ! -f "$JS_FILE" ] && continue
    IMG_URL=$(grep 'image_url' "$JS_FILE" | head -1 | sed "s/.*image_url:[[:space:]]*['\"]//;s/['\"].*//" || true)
    [ -z "$IMG_URL" ] && continue
    BASENAME=$(basename "$IMG_URL")
    # Resolve disk path from actual image_url (supports shared-image mode — W16+ Easy may point to /images/weekN/)
    DISK_PATH="public${IMG_URL}"
    if [ ! -f "$DISK_PATH" ]; then
      echo -e "   ${RED}❌ FAIL ($(basename $JS_FILE)): image_url='$IMG_URL' → file not found: $DISK_PATH${NC}"
      echo -e "   ${YELLOW}FIX: image_url must use w${WEEK_INT} (no zero-padding), e.g. read_cover_w${WEEK_INT}.jpg — NOT w0${WEEK_INT}${NC}"
      COVER_ERRORS=$((COVER_ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS: $(basename $JS_FILE) → $BASENAME exists${NC}"
    fi
  done
  [ $COVER_ERRORS -gt 0 ] && ERRORS=$((ERRORS+COVER_ERRORS))
fi
echo ""
echo -e "${CYAN}${BOLD}────────────────── CONTENT COUNT / REGISTRATION CHECKS ────────────${NC}"
echo -e "${CYAN}   Validates exercise counts, GameHub registry, AI Tutor tab wiring${NC}"
echo -e "${CYAN}${BOLD}────────────────────────────────────────────────────────────────────${NC}"
echo ""

# =============================================================================
# CHECK 27: grammar.js must have exactly 20 exercises per mode
# =============================================================================
echo -e "${BOLD}[CHECK 27] grammar.js — must have exactly 20 exercises (both modes)${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  GR_FILE="${dir}/grammar.js"
  if [ ! -f "$GR_FILE" ]; then
    echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): grammar.js not found${NC}"; WARNINGS=$((WARNINGS+1)); continue
  fi
  # Count exercises by loading the file and looking at m.default.exercises
  EX_COUNT=$(node -e "
    try {
      const m = require('./$GR_FILE');
      const arr = m.default.exercises || m.default.grammar || m.default.questions || [];
      console.log(arr.length);
    } catch(e) { console.log(0); }
  " 2>/dev/null || echo "0")
  if [ "$EX_COUNT" -ne 20 ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): grammar.js has $EX_COUNT exercises (expected exactly 20)${NC}"
    echo -e "   ${YELLOW}FIX: Add/remove exercises until count = 20 in ${GR_FILE}${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): grammar.js has exactly $EX_COUNT exercises${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 28: gameAdaptation.js — weekGamesMap must register week N
# =============================================================================
echo -e "${BOLD}[CHECK 28] gameAdaptation.js — week $WEEK_INT must be in weekGamesMap${NC}"
GA_FILE="src/config/gameAdaptation.js"
if [ ! -f "$GA_FILE" ]; then
  echo -e "   ${RED}❌ FAIL: src/config/gameAdaptation.js not found${NC}"; ERRORS=$((ERRORS+1))
else
  GA_ERRS=""
  ADV_IMPORT=$(grep -c "week${WEEK_INT}GamesAdvanced" "$GA_FILE" || true)
  EASY_IMPORT=$(grep -c "week${WEEK_INT}GamesEasy" "$GA_FILE" || true)
  MAP_ENTRY=$(grep -cE "[[:space:]]+${WEEK_INT}: \{" "$GA_FILE" || true)
  [ "$ADV_IMPORT" -eq 0 ] && GA_ERRS="${GA_ERRS}\n   • Missing import: week${WEEK_INT}GamesAdvanced"
  [ "$EASY_IMPORT" -eq 0 ] && GA_ERRS="${GA_ERRS}\n   • Missing import: week${WEEK_INT}GamesEasy"
  [ "$MAP_ENTRY" -eq 0 ] && GA_ERRS="${GA_ERRS}\n   • weekGamesMap missing entry for week $WEEK_INT"
  if [ -n "$GA_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL: gameAdaptation.js not wired for week $WEEK_INT:${NC}"
    echo -e "$GA_ERRS" | while IFS= read -r line; do echo -e "   ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: Add imports + register '${WEEK_INT}: { advanced: week${WEEK_INT}GamesAdvanced, easy: week${WEEK_INT}GamesEasy }' in weekGamesMap${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: gameAdaptation.js registers week $WEEK_INT (imports + weekGamesMap entry)${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 29: StoryMissionTab.jsx + FreeTalkTab.jsx must import week N real data
# =============================================================================
echo -e "${BOLD}[CHECK 29] AI Tutor tabs — StoryMissionTab + FreeTalkTab must include week $WEEK_INT${NC}"
for tab_file in \
  "src/modules/ai_tutor/tabs/StoryMissionTab.jsx" \
  "src/modules/ai_tutor/tabs/FreeTalkTab.jsx"; do
  TAB_NAME=$(basename "$tab_file" .jsx)
  if [ ! -f "$tab_file" ]; then
    echo -e "   ${YELLOW}⚠️  SKIP: $TAB_NAME not found${NC}"; WARNINGS=$((WARNINGS+1)); continue
  fi
  HAS_IMPORT=$(grep -c "week${WEEK_INT}RealData\|week_${WEEK_PAD}_real" "$tab_file" || true)
  if [ "$HAS_IMPORT" -eq 0 ]; then
    echo -e "   ${RED}❌ FAIL ($TAB_NAME): week $WEEK_INT not wired — AI Tutor will fall back to older week content${NC}"
    echo -e "   ${YELLOW}FIX: Add import week${WEEK_INT}RealData + ternary 'weekNumber === $WEEK_INT ? week${WEEK_INT}RealData :' in $tab_file${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($TAB_NAME): week $WEEK_INT import/ternary present ($HAS_IMPORT occurrence(s))${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 30: games.js — show_tell, make_sentence, ask_me must have content (W28-32 format)
# =============================================================================
echo -e "${BOLD}[CHECK 30] games.js — W28-32 format: show_tell/make_sentence/ask_me + no W16 detail_map (both modes)${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  GM_FILE="${dir}/games.js"
  if [ ! -f "$GM_FILE" ]; then
    echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): games.js not found${NC}"; WARNINGS=$((WARNINGS+1)); continue
  fi
  GM_ERRS=""
  # W28-32 required sections
  grep -q "show_tell:" "$GM_FILE" || GM_ERRS="${GM_ERRS}\n   • Missing: show_tell section"
  grep -q "make_sentence:" "$GM_FILE" || GM_ERRS="${GM_ERRS}\n   • Missing: make_sentence section"
  grep -q "ask_me:" "$GM_FILE" || GM_ERRS="${GM_ERRS}\n   • Missing: ask_me section"
  # W16 format (DEPRECATED) — must NOT be present in W28+
  if [ "$WEEK_INT" -ge 28 ]; then
    grep -q "detail_map:" "$GM_FILE" && GM_ERRS="${GM_ERRS}\n   • DEPRECATED W16 format: detail_map found (W28-32 uses simple show_tell without detail_map)"
    grep -q "emoji_map:" "$GM_FILE" && GM_ERRS="${GM_ERRS}\n   • DEPRECATED W16 format: emoji_map found"
    grep -q "distractor_map:" "$GM_FILE" && GM_ERRS="${GM_ERRS}\n   • DEPRECATED W16 format: distractor_map found"
  fi
  # Answer count check
  ANSWER_COUNT=$(grep -c "answer:" "$GM_FILE" || true)
  [ "$ANSWER_COUNT" -lt 10 ] && GM_ERRS="${GM_ERRS}\n   • Too few sentence answers ($ANSWER_COUNT 'answer:' entries — need >= 10 across make_sentence + ask_me)"
  if [ -n "$GM_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): games.js content issues:${NC}"
    echo -e "$GM_ERRS" | while IFS= read -r line; do echo -e "   ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: Use W28-32 format (no detail_map/emoji_map/distractor_map). Reference: weeks/week_32/games.js${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): games.js W28-32 format OK ($ANSWER_COUNT answer entries)${NC}"
  fi
done
echo ""
# =============================================================================
# CHECK 31: AI Tutor week_NN_real.js — must have conversation_cards array
# =============================================================================
echo -e "${BOLD}[CHECK 31] AI Tutor week_${WEEK_PAD}_real.js — must have conversation_cards (>= 3 cards)${NC}"
if [ ! -f "$AI_TUTOR" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $AI_TUTOR not yet created${NC}"; WARNINGS=$((WARNINGS+1))
else
  CC_COUNT=$(grep -c "id:.*_cards\|id: \"[a-z_]*\"" "$AI_TUTOR" 2>/dev/null || true)
  HAS_CC=$(grep -c "conversation_cards:" "$AI_TUTOR" 2>/dev/null || true)
  CARD_IDS=$(grep -c "completion_message" "$AI_TUTOR" 2>/dev/null || true)
  if [ "$HAS_CC" -eq 0 ]; then
    echo -e "   ${RED}❌ FAIL: Missing conversation_cards array in $AI_TUTOR${NC}"
    echo -e "   ${YELLOW}FIX: Add conversation_cards: [ ...3 cards... ] at end of file (before closing });)${NC}"
    ERRORS=$((ERRORS+1))
  elif [ "$CARD_IDS" -lt 3 ]; then
    echo -e "   ${RED}❌ FAIL: conversation_cards has only $CARD_IDS card(s) — need >= 3${NC}"
    echo -e "   ${YELLOW}FIX: Add at least 3 cards each with completion_message${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: conversation_cards present with $CARD_IDS card(s)${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 32: Vocab image_url files must exist on disk (catches vocab added after prompts)
# =============================================================================
echo -e "${BOLD}[CHECK 32] vocab.js — all image_url files must exist on disk${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  VOCAB_FILE="${dir}/vocab.js"
  [ ! -f "$VOCAB_FILE" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): vocab.js not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue
  VOCAB_ERRS=""
  while IFS= read -r img_path; do
    # Strip quotes and trim
    clean=$(echo "$img_path" | grep -o '"/images/[^"]*"' | tr -d '"')
    [ -z "$clean" ] && continue
    disk_path="public${clean}"
    [ ! -f "$disk_path" ] && VOCAB_ERRS="${VOCAB_ERRS}\n   • MISSING on disk: $clean"
  done < <(grep "image_url" "$VOCAB_FILE")
  if [ -n "$VOCAB_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): vocab.js references images that don't exist:${NC}"
    echo -e "$VOCAB_ERRS" | while IFS= read -r line; do echo -e "   ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX: Either add the image to public/images/week${WEEK}/ or reuse an existing image${NC}"
    echo -e "   ${YELLOW}NOTE: If you added vocab words AFTER creating image prompts, update the prompt file too${NC}"
    ERRORS=$((ERRORS+1))
  else
    VOCAB_IMG_COUNT=$(grep -c "image_url" "$VOCAB_FILE" || true)
    echo -e "   ${GREEN}✅ PASS ($LABEL): All $VOCAB_IMG_COUNT vocab images exist on disk${NC}"
  fi
done
echo ""

# =============================================================================
# CHECK 33: daily_watch.js — video IDs must not duplicate previous week
# =============================================================================
echo -e "${BOLD}[CHECK 33] daily_watch.js — no duplicate video IDs from previous week (week $((WEEK_INT-1)))${NC}"
PREV_WEEK=$((WEEK_INT - 1))
PREV_WEEK_PAD=$(printf "%02d" $PREV_WEEK)
PREV_DW="src/data/weeks/week_${PREV_WEEK_PAD}/daily_watch.js"
DW_CURR="src/data/weeks/week_${WEEK_PAD}/daily_watch.js"
if [ ! -f "$PREV_DW" ] || [ ! -f "$DW_CURR" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: Previous week daily_watch not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  PREV_IDS=$(grep -o 'videoId: *"[^"]*"' "$PREV_DW" | grep -o '"[^"]*"$' | tr -d '"' | sort)
  CURR_IDS=$(grep -o 'videoId: *"[^"]*"' "$DW_CURR" | grep -o '"[^"]*"$' | tr -d '"' | sort)
  DUPES=""
  while IFS= read -r vid; do
    echo "$PREV_IDS" | grep -qx "$vid" && DUPES="${DUPES} $vid"
  done <<< "$CURR_IDS"
  if [ -n "$DUPES" ]; then
    echo -e "   ${RED}❌ FAIL: Videos duplicated from Week ${PREV_WEEK_PAD}:${DUPES}${NC}"
    echo -e "   ${YELLOW}FIX: Run: node tools/update_videos.js ${WEEK_INT} --reset${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: No duplicate video IDs from Week ${PREV_WEEK_PAD}${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 34: vocab.js word count must match image prompt Filename count
# =============================================================================
echo -e "${BOLD}[CHECK 34] vocab.js word count vs image prompt vocab count must match${NC}"
ADV_PROMPTS_CHECK="${IMG_PROMPTS_DIR}/week_${WEEK_PAD}_image_prompts.txt"
VOCAB_FILE_CHECK="${ADV_DIR}/vocab.js"
if [ ! -f "$ADV_PROMPTS_CHECK" ] || [ ! -f "$VOCAB_FILE_CHECK" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: prompt file or vocab.js not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  # Count vocab entries — use image_url lines which appear exactly once per entry
  PROMPT_VOCAB_COUNT=$(grep "Filename:" "$ADV_PROMPTS_CHECK" | grep -v "wordpower_\|wp_\|barmodel_\|cover_\|_cover" | wc -l)
  DATA_VOCAB_COUNT=$(grep -c "image_url:" "$VOCAB_FILE_CHECK" || true)
  if [ "$PROMPT_VOCAB_COUNT" -ne "$DATA_VOCAB_COUNT" ]; then
    echo -e "   ${RED}❌ FAIL: vocab.js has $DATA_VOCAB_COUNT words but image prompt has $PROMPT_VOCAB_COUNT vocab images${NC}"
    echo -e "   ${YELLOW}FIX: If you added vocab words after images were generated, either:${NC}"
    echo -e "   ${YELLOW}  (a) Reuse an existing image_url for the new word, OR${NC}"
    echo -e "   ${YELLOW}  (b) Add the new word as prompt #$(($PROMPT_VOCAB_COUNT+1)) to the prompt file and generate the image${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: vocab.js ($DATA_VOCAB_COUNT words) matches image prompts ($PROMPT_VOCAB_COUNT vocab images)${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 35: Easy vocab.js word count must match Advanced vocab.js (BUG-W22-B)
# =============================================================================
echo -e "${BOLD}[CHECK 35] vocab.js — Easy mode word count must equal Advanced word count${NC}"
ADV_VOCAB_C35="${ADV_DIR}/vocab.js"
EASY_VOCAB_C35="${EASY_DIR}/vocab.js"
if [ ! -f "$ADV_VOCAB_C35" ] || [ ! -f "$EASY_VOCAB_C35" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: vocab.js not found in one or both modes${NC}"; WARNINGS=$((WARNINGS+1))
else
  ADV_VCOUNT=$(grep -c "image_url:" "$ADV_VOCAB_C35" || true)
  EASY_VCOUNT=$(grep -c "image_url:" "$EASY_VOCAB_C35" || true)
  if [ "$ADV_VCOUNT" -ne "$EASY_VCOUNT" ]; then
    echo -e "   ${RED}❌ FAIL: Advanced has $ADV_VCOUNT vocab words but Easy has $EASY_VCOUNT — must match${NC}"
    echo -e "   ${YELLOW}FIX (BUG-W22-B): Easy vocab.js must include ALL syllabus words (same count as Advanced). W22 bug: easy had 10, should have been 13.${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: Both modes have $ADV_VCOUNT vocab words${NC}"
  fi
fi
echo ""

# CHECK 36: Vocab cross-contamination — read.js must use this week's vocab (BUG-W22-A)
# =============================================================================
# dictation.js + shadowing.js are exact copies of read.js sentences — check read.js as the source.
echo -e "${BOLD}[CHECK 36] Vocab cross-contamination — read.js must use week $WEEK_INT vocab words${NC}"
ADV_VOCAB_C36="${ADV_DIR}/vocab.js"
ADV_READ_C36="${ADV_DIR}/read.js"
if [ ! -f "$ADV_VOCAB_C36" ] || [ ! -f "$ADV_READ_C36" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: vocab.js or read.js not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  VOCAB_WORDS=$(grep -E '^\s+word:' "$ADV_VOCAB_C36" | sed -E 's/.*word:[[:space:]]*"([^"]+)".*/\1/' | tr '[:upper:]' '[:lower:]')
  TOTAL_VOCAB=$(echo "$VOCAB_WORDS" | grep -c "." || true)
  MATCH_COUNT=0
  while IFS= read -r vword; do
    [ -z "$vword" ] && continue
    grep -qi "$vword" "$ADV_READ_C36" 2>/dev/null && MATCH_COUNT=$((MATCH_COUNT+1))
  done <<< "$VOCAB_WORDS"
  MIN_MATCH=$(( TOTAL_VOCAB * 5 / 10 ))
  if [ "$MATCH_COUNT" -lt "$MIN_MATCH" ]; then
    echo -e "   ${RED}❌ FAIL: Only $MATCH_COUNT/$TOTAL_VOCAB vocab words appear in read.js — need >= $MIN_MATCH${NC}"
    echo -e "   ${YELLOW}FIX (BUG-W22-A): read.js was copied from previous week — rewrite story with this week's vocab. dictation+shadowing will follow automatically.${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: $MATCH_COUNT/$TOTAL_VOCAB vocab words confirmed in read.js${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 37: explore.js — CLIL quality (BUG-W22-D): >= 8 bold markers + content length
# =============================================================================
echo -e "${BOLD}[CHECK 37] explore.js — CLIL quality: >= 8 **bold** vocab markers, content >= 100 words${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  EXP_FILE_C37="${dir}/explore.js"
  [ ! -f "$EXP_FILE_C37" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): explore.js not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  EXP_ERRS=""
  BOLD_COUNT=$(grep -o '\*\*' "$EXP_FILE_C37" | wc -l | tr -d ' ')
  [ "$BOLD_COUNT" -lt 16 ] && EXP_ERRS="${EXP_ERRS}\n   • Only $BOLD_COUNT ** markers found — need >= 16 (= 8 bolded words). Blueprint requires 10."

  CONTENT_WORDS=$(awk '/content_en:/,/`,$/' "$EXP_FILE_C37" | grep -oE '[A-Za-z]+' | wc -w | tr -d ' ')
  [ "$CONTENT_WORDS" -lt 100 ] && EXP_ERRS="${EXP_ERRS}\n   • content_en has only ~$CONTENT_WORDS words — CLIL article must be >= 100 words"

  CQ_COUNT=$(grep -c '"id":' "$EXP_FILE_C37" 2>/dev/null || grep -c 'id: [0-9]' "$EXP_FILE_C37" 2>/dev/null || true)
  [ "$CQ_COUNT" -lt 3 ] && EXP_ERRS="${EXP_ERRS}\n   • Missing check_questions: only $CQ_COUNT id entries (need 3)"

  if [ -n "$EXP_ERRS" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): explore.js CLIL quality issues:${NC}"
    echo -e "$EXP_ERRS" | while IFS= read -r line; do echo -e "   ${RED}$line${NC}"; done
    echo -e "   ${YELLOW}FIX (BUG-W22-D): explore.js must be a CLIL non-fiction article (real-world science), NOT grammar exercises. Bold all 10 vocab words in the text.${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): explore.js is a proper CLIL article (~$BOLD_COUNT ** markers, ~$CONTENT_WORDS words)${NC}"
  fi
done
echo ""

# CHECK 38: Grammar tense consistency — read.js tense must match week grammar_focus (BUG-W22-C)
# =============================================================================
# dictation.js + shadowing.js are exact copies of read.js sentences — checking read.js is sufficient.
echo -e "${BOLD}[CHECK 38] Grammar tense — read.js tense must match week grammar_focus${NC}"
AI_TUTOR_C38="src/data/weeks/week_${WEEK_PAD}_real.js"
if [ ! -f "$AI_TUTOR_C38" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: week_${WEEK_PAD}_real.js not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  GRAMMAR_FOCUS=$(grep -m1 "grammar_focus:" "$AI_TUTOR_C38" | sed -E 's/.*grammar_focus:[[:space:]]*"([^"]+)".*/\1/' | tr '[:upper:]' '[:lower:]')
  if echo "$GRAMMAR_FOCUS" | grep -qiE "past|simple past|-ed"; then
    for dir in "$ADV_DIR" "$EASY_DIR"; do
      [ ! -d "$dir" ] && continue
      LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
      READ_C38="${dir}/read.js"
      if [ ! -f "$READ_C38" ]; then
        echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): read.js not found${NC}"; continue
      fi
      ED_COUNT=$(grep -oiE "[a-z]+ed[\"' ,.]" "$READ_C38" | wc -l | tr -d ' ')
      if [ "$ED_COUNT" -lt 3 ]; then
        echo -e "   ${RED}❌ FAIL ($LABEL): grammar_focus='$GRAMMAR_FOCUS' (past) but read.js has only $ED_COUNT -ed verb form(s) — need >= 3${NC}"
        echo -e "   ${YELLOW}FIX (BUG-W22-C): read.js story was written in wrong tense. Rewrite in Simple Past — dictation+shadowing will follow automatically.${NC}"
        ERRORS=$((ERRORS+1))
      else
        echo -e "   ${GREEN}✅ PASS ($LABEL): Past-tense week — $ED_COUNT -ed forms confirmed in read.js${NC}"
      fi
    done
  else
    echo -e "   ${GREEN}✅ PASS: grammar_focus='$GRAMMAR_FOCUS' — past-tense check not applicable${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 39: conversation_cards in week_NN_real.js must use exchanges format (BUG-W23-A)
# =============================================================================
echo -e "${BOLD}[CHECK 39] conversation_cards — must have exchanges array (not prompt/starter format)${NC}"
AI_TUTOR_39="src/data/weeks/week_${WEEK_PAD}_real.js"
if [ ! -f "$AI_TUTOR_39" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $AI_TUTOR_39 not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  CARDS_BLOCK=$(grep -c "conversation_cards" "$AI_TUTOR_39" || true)
  if [ "$CARDS_BLOCK" -eq 0 ]; then
    echo -e "   ${YELLOW}⚠️  SKIP: no conversation_cards found in $AI_TUTOR_39${NC}"; WARNINGS=$((WARNINGS+1))
  else
    EXCHANGES_COUNT=$(grep -c "exchanges:" "$AI_TUTOR_39" || true)
    OLD_FORMAT_COUNT=$(grep -c "prompt_en:\|starter:" "$AI_TUTOR_39" || true)
    if [ "$OLD_FORMAT_COUNT" -gt 0 ]; then
      echo -e "   ${RED}❌ FAIL: conversation_cards uses legacy prompt_en/starter format — update to exchanges format${NC}"
      echo -e "   ${YELLOW}   FIX: Each card must have: id(string), title, emoji, theme, difficulty, exchanges:[{ai,options}], completion_message${NC}"
      echo -e "   ${YELLOW}   REF: week_22_real.js conversation_cards (lines ~380-460)${NC}"
      ERRORS=$((ERRORS+1))
    elif [ "$EXCHANGES_COUNT" -lt 3 ]; then
      echo -e "   ${RED}❌ FAIL: conversation_cards has $EXCHANGES_COUNT exchanges field(s) — need >= 3 (one per card)${NC}"
      echo -e "   ${YELLOW}   FIX: Each of the 3 cards must have an exchanges: [...] array${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS: conversation_cards has $EXCHANGES_COUNT exchanges entries (correct format)${NC}"
    fi
  fi
fi
echo ""

# =============================================================================
# CHECK 40: image_prompts file must NOT contain bar model entries (BUG-W23-B)
# Bar models are auto-generated by generate_logiclab_barmodels.py, not AI image prompts
# =============================================================================
echo -e "${BOLD}[CHECK 40] image_prompts — must not contain barmodel_ entries${NC}"
PROMPT_FILE_40="Production_FINAL/IMAGE PROMPTS/week_${WEEK_PAD}_image_prompts.txt"
if [ ! -f "$PROMPT_FILE_40" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $PROMPT_FILE_40 not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  BARMODEL_LINES=$(grep -c "barmodel_\|bar_model_" "$PROMPT_FILE_40" || true)
  if [ "$BARMODEL_LINES" -gt 0 ]; then
    echo -e "   ${RED}❌ FAIL: image_prompts file has $BARMODEL_LINES bar model entr(ies) — remove them${NC}"
    echo -e "   ${YELLOW}   Bar models are auto-generated: python3 generate_logiclab_barmodels.py $WEEK_INT${NC}"
    echo -e "   ${YELLOW}   Image prompts = vocab images + wordpower images + cover images ONLY${NC}"
    grep -n "barmodel_\|bar_model_" "$PROMPT_FILE_40" | head -5 | while IFS= read -r line; do
      echo -e "      ${RED}$line${NC}"
    done
    ERRORS=$((ERRORS+1))
  else
    PROMPT_COUNT=$(grep -c "Filename:" "$PROMPT_FILE_40" || true)
    echo -e "   ${GREEN}✅ PASS: No bar model entries in image_prompts ($PROMPT_COUNT prompts — vocab/wordpower/covers only)${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 41: Bar model images must be uploaded to R2 CDN (BUG-W23-C)
# After generate_logiclab_barmodels.py, must run upload_week_images_r2.py
# =============================================================================
echo -e "${BOLD}[CHECK 41] Bar model images — must be uploaded to R2 CDN${NC}"
BAR_LOCAL_ADV="public/images/week${WEEK_INT}/barmodel_w${WEEK_INT}_adv_p1_v1.jpg"
if [ ! -f "$BAR_LOCAL_ADV" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: No local bar model images for week $WEEK_INT (not yet generated or week < 16)${NC}"
  WARNINGS=$((WARNINGS+1))
else
  BAR_CDN_BASE="https://pub-6b5486dcbb554a6694b6c7032a43dcae.r2.dev/images/week${WEEK_INT}"
  BAR_MISSING=0
  for MODE in adv easy; do
    for NUM in 1 2 3 4 5; do
      LOCAL_F="public/images/week${WEEK_INT}/barmodel_w${WEEK_INT}_${MODE}_p${NUM}_v1.jpg"
      R2_URL="${BAR_CDN_BASE}/barmodel_w${WEEK_INT}_${MODE}_p${NUM}_v1.jpg"
      if [ -f "$LOCAL_F" ]; then
        HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 --range 0-0 "$R2_URL" 2>/dev/null || echo "000")
        if [ "$HTTP_CODE" != "206" ] && [ "$HTTP_CODE" != "200" ]; then
          echo -e "   ${RED}❌ NOT on R2: barmodel_w${WEEK_INT}_${MODE}_p${NUM}_v1.jpg (HTTP $HTTP_CODE)${NC}"
          BAR_MISSING=$((BAR_MISSING+1))
        fi
      fi
    done
  done
  if [ "$BAR_MISSING" -gt 0 ]; then
    echo -e "   ${RED}❌ FAIL: $BAR_MISSING bar model image(s) missing from R2 CDN${NC}"
    echo -e "   ${YELLOW}   FIX: python3 tools/upload_week_images_r2.py $WEEK_INT${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: All bar model images confirmed on R2 CDN${NC}"
  fi
fi
echo ""


# =============================================================================
# CHECK 39: conversation_cards in week_NN_real.js must use exchanges format (BUG-W23-A)
# =============================================================================
echo -e "${BOLD}[CHECK 39] conversation_cards — must have exchanges array (not prompt/starter format)${NC}"
AI_TUTOR_39="src/data/weeks/week_${WEEK_PAD}_real.js"
if [ ! -f "$AI_TUTOR_39" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $AI_TUTOR_39 not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  CARDS_BLOCK=$(grep -c "conversation_cards" "$AI_TUTOR_39" || true)
  if [ "$CARDS_BLOCK" -eq 0 ]; then
    echo -e "   ${YELLOW}⚠️  SKIP: no conversation_cards found in $AI_TUTOR_39${NC}"; WARNINGS=$((WARNINGS+1))
  else
    EXCHANGES_COUNT=$(grep -c "exchanges:" "$AI_TUTOR_39" || true)
    OLD_FORMAT_COUNT=$(grep -c "prompt_en:\|starter:" "$AI_TUTOR_39" || true)
    if [ "$OLD_FORMAT_COUNT" -gt 0 ]; then
      echo -e "   ${RED}❌ FAIL: conversation_cards uses legacy prompt_en/starter format — update to exchanges format${NC}"
      echo -e "   ${YELLOW}   FIX: Each card must have: id(string), title, emoji, theme, difficulty, exchanges:[{ai,options}], completion_message${NC}"
      echo -e "   ${YELLOW}   REF: week_22_real.js conversation_cards (lines ~380-460)${NC}"
      ERRORS=$((ERRORS+1))
    elif [ "$EXCHANGES_COUNT" -lt 3 ]; then
      echo -e "   ${RED}❌ FAIL: conversation_cards has $EXCHANGES_COUNT exchanges field(s) — need >= 3 (one per card)${NC}"
      echo -e "   ${YELLOW}   FIX: Each of the 3 cards must have an exchanges: [...] array${NC}"
      ERRORS=$((ERRORS+1))
    else
      echo -e "   ${GREEN}✅ PASS: conversation_cards has $EXCHANGES_COUNT exchanges entries (correct format)${NC}"
    fi
  fi
fi
echo ""

# =============================================================================
# CHECK 40: image_prompts file must NOT contain bar model entries (BUG-W23-B)
# Bar models are auto-generated by generate_logiclab_barmodels.py, not AI image prompts
# =============================================================================
echo -e "${BOLD}[CHECK 40] image_prompts — must not contain barmodel_ entries${NC}"
PROMPT_FILE_40="Production_FINAL/IMAGE PROMPTS/week_${WEEK_PAD}_image_prompts.txt"
if [ ! -f "$PROMPT_FILE_40" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: $PROMPT_FILE_40 not found${NC}"; WARNINGS=$((WARNINGS+1))
else
  BARMODEL_LINES=$(grep -c "barmodel_\|bar_model_" "$PROMPT_FILE_40" || true)
  if [ "$BARMODEL_LINES" -gt 0 ]; then
    echo -e "   ${RED}❌ FAIL: image_prompts file has $BARMODEL_LINES bar model entr(ies) — remove them${NC}"
    echo -e "   ${YELLOW}   Bar models are auto-generated: python3 generate_logiclab_barmodels.py $WEEK_INT${NC}"
    echo -e "   ${YELLOW}   Image prompts = vocab images + wordpower images + cover images ONLY${NC}"
    grep -n "barmodel_\|bar_model_" "$PROMPT_FILE_40" | head -5 | while IFS= read -r line; do
      echo -e "      ${RED}$line${NC}"
    done
    ERRORS=$((ERRORS+1))
  else
    PROMPT_COUNT=$(grep -c "Filename:" "$PROMPT_FILE_40" || true)
    echo -e "   ${GREEN}✅ PASS: No bar model entries in image_prompts ($PROMPT_COUNT prompts — vocab/wordpower/covers only)${NC}"
  fi
fi
echo ""

# =============================================================================
# CHECK 41: Bar model images must be uploaded to R2 CDN (BUG-W23-C)
# After generate_logiclab_barmodels.py, must run upload_week_images_r2.py
# =============================================================================
echo -e "${BOLD}[CHECK 41] Bar model images — must be uploaded to R2 CDN${NC}"
BAR_LOCAL_ADV="public/images/week${WEEK_INT}/barmodel_w${WEEK_INT}_adv_p1_v1.jpg"
if [ ! -f "$BAR_LOCAL_ADV" ]; then
  echo -e "   ${YELLOW}⚠️  SKIP: No local bar model images for week $WEEK_INT (not yet generated or week < 16)${NC}"
  WARNINGS=$((WARNINGS+1))
else
  BAR_CDN_BASE="https://pub-6b5486dcbb554a6694b6c7032a43dcae.r2.dev/images/week${WEEK_INT}"
  BAR_MISSING=0
  for MODE in adv easy; do
    for NUM in 1 2 3 4 5; do
      LOCAL_F="public/images/week${WEEK_INT}/barmodel_w${WEEK_INT}_${MODE}_p${NUM}_v1.jpg"
      R2_URL="${BAR_CDN_BASE}/barmodel_w${WEEK_INT}_${MODE}_p${NUM}_v1.jpg"
      if [ -f "$LOCAL_F" ]; then
        HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" --max-time 5 --range 0-0 "$R2_URL" 2>/dev/null || echo "000")
        if [ "$HTTP_CODE" != "206" ] && [ "$HTTP_CODE" != "200" ]; then
          echo -e "   ${RED}❌ NOT on R2: barmodel_w${WEEK_INT}_${MODE}_p${NUM}_v1.jpg (HTTP $HTTP_CODE)${NC}"
          BAR_MISSING=$((BAR_MISSING+1))
        fi
      fi
    done
  done
  if [ "$BAR_MISSING" -gt 0 ]; then
    echo -e "   ${RED}❌ FAIL: $BAR_MISSING bar model image(s) missing from R2 CDN${NC}"
    echo -e "   ${YELLOW}   FIX: python3 tools/upload_week_images_r2.py $WEEK_INT${NC}"
    ERRORS=$((ERRORS+1))
  else
    echo -e "   ${GREEN}✅ PASS: All bar model images confirmed on R2 CDN${NC}"
  fi
fi
echo ""





# =============================================================================

# =============================================================================
# CHECK 42: dictation.js + shadowing.js content must be extracted from read.js
# content_en must be EXACT copy of read.js; sentences must be verbatim from story
# =============================================================================
echo -e "${BOLD}[CHECK 42] dictation/shadowing sentences — must come from read.js${NC}"
export _GATE_WEEK="$WEEK_PAD"
python3 << 'PYEOF'
import re, sys, os

week = os.environ.get('_GATE_WEEK', '')
BASE = os.getcwd()
errors = []


def extract_content_en(js_text):
    # Try backtick (modern ES6 template literal)
    m = re.search(r'\bcontent_en:\s*`((?:[^`\\]|\\.)*)`', js_text)
    if m:
        return m.group(1).replace('\\n', '\n')
    # Fall back to double-quoted string
    m = re.search(r'\bcontent_en:\s*"((?:[^"\\]|\\.)*)"', js_text)
    if m:
        return m.group(1).replace('\\n', '\n')
    return None


def extract_sentence_texts(js_text):
    # Sentences use double-quoted strings
    return re.findall(r'\btext:\s*"((?:[^"\\]|\\.)*)"', js_text)


def strip_bold(text):
    return text.replace('**', '')


def normalize(text):
    return re.sub(r'\s+', ' ', text.strip())


def check_pair(read_path, check_path, label):
    if not os.path.exists(read_path) or not os.path.exists(check_path):
        return
    with open(read_path, encoding='utf-8') as f:
        read_js = f.read()
    with open(check_path, encoding='utf-8') as f:
        check_js = f.read()
    read_content = extract_content_en(read_js)
    if not read_content:
        return
    read_clean = normalize(strip_bold(read_content))
    file_content = extract_content_en(check_js)
    if file_content is not None:
        if normalize(strip_bold(file_content)) != read_clean:
            errors.append(f"{label}: content_en does not match read.js")
    for sent in extract_sentence_texts(check_js):
        # Strip **bold** first, then normalize (sentence text has bold but read_clean doesn't)
        clean = normalize(strip_bold(sent))
        # Also try stripping surrounding quotes for sentences inside quotes
        clean_stripped = clean.strip("'\"")
        if clean not in read_clean and clean_stripped not in read_clean:
            errors.append(f"{label}: sentence not in read.js story: \"{sent[:55]}\"")


for mode, week_dir in [("ADV", f"src/data/weeks/week_{week}"), ("EASY", f"src/data/weeks_easy/week_{week}")]:
    read_f = os.path.join(BASE, week_dir, "read.js")
    dict_f = os.path.join(BASE, week_dir, "dictation.js")
    shad_f = os.path.join(BASE, week_dir, "shadowing.js")
    check_pair(read_f, dict_f, f"{mode}/dictation.js")
    check_pair(read_f, shad_f, f"{mode}/shadowing.js")

for e in errors:
    print(f"   \u274c GATE FAILED: {e}")
if not errors:
    print("   \u2705 PASS: dictation/shadowing sentences verified against read.js")
else:
    sys.exit(1)
PYEOF
if [ $? -ne 0 ]; then
  ERRORS=$((ERRORS+1))
fi
echo ""


# =============================================================================
# CHECK 43: content:lint -- read.js + explore.js quality gate
# Enforces N-05 (word count), B-01 (bold markers), N-01 (language purity), N-06 (banned vocab)
# =============================================================================
echo -e "${BOLD}[CHECK 43] content:lint --- read.js + explore.js quality${NC}"
node tools/content_lint.mjs --week "$WEEK_INT" --errors-only 2>&1
if [ $? -ne 0 ]; then
  ERRORS=$((ERRORS+1))
  echo -e "   ${RED}❌ FAIL: content:lint errors -- see above${NC}"
  echo -e "   ${YELLOW}FIX: CONTENT_RULES_READ_EXPLORE.md -- E1=word count, E2=bold markers, E3=no Vietnamese in content_en, E4=banned vocab Easy Phase 1${NC}"
else
  echo -e "   ${GREEN}✅ PASS: read.js + explore.js content quality OK${NC}"
fi
echo ""

# =============================================================================
# CHECK 44: vocab.js audio field names — must use correct schema
# audio_word, audio_definition, audio_example, audio_collocation, audio_collocation_0/1/2...
# WRONG: audio_def, audio_coll, audio_coll_0, audio_url, audio_coll_phrase
# Bug: wrong field names cause silent TTS failures (undefined URL → fallback → garbage audio)
# =============================================================================
echo -e "${BOLD}[CHECK 44] vocab.js audio fields — correct schema (audio_word/definition/example/collocation)${NC}"
Vocab_FIELD_ERRS=0
AUDIT_PY="/Users/binhnguyen/.claude/commands/audit_check.py"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  VOCAB="${dir}/vocab.js"
  [ ! -f "$VOCAB" ] && echo -e "   ${YELLOW}⚠️  SKIP ($LABEL): vocab.js not found${NC}" && WARNINGS=$((WARNINGS+1)) && continue

  # Bad field names to flag
  BAD_DEFS=$(grep -n "audio_def\b" "$VOCAB" | grep -v "//" | head -3 || true)
  BAD_COLLS=$(grep -n "audio_coll\b" "$VOCAB" | grep -v "//" | grep -v "audio_collocation" | head -3 || true)
  BAD_URLS=$(grep -n "audio_url:" "$VOCAB" | grep -v "//" | head -3 || true)

  if [ -n "$BAD_DEFS" ] || [ -n "$BAD_COLLS" ] || [ -n "$BAD_URLS" ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): vocab.js has wrong audio field names:${NC}"
    [ -n "$BAD_DEFS"  ] && echo "$BAD_DEFS"  | while IFS= read -r l; do echo -e "   ${RED}   → audio_def (should be audio_definition): $l${NC}"; done
    [ -n "$BAD_COLLS" ] && echo "$BAD_COLLS" | while IFS= read -r l; do echo -e "   ${RED}   → audio_coll (should be audio_collocation): $l${NC}"; done
    [ -n "$BAD_URLS"  ] && echo "$BAD_URLS"  | while IFS= read -r l; do echo -e "   ${RED}   → audio_url (should be audio_word/audio_definition/etc): $l${NC}"; done
    echo -e "   ${YELLOW}   FIX: Use audio_word, audio_definition, audio_example, audio_collocation, audio_collocation_0/1/2...${NC}"
    Vocab_FIELD_ERRS=$((Vocab_FIELD_ERRS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): vocab.js audio field names correct${NC}"
  fi
done
[ $Vocab_FIELD_ERRS -gt 0 ] && ERRORS=$((ERRORS+Vocab_FIELD_ERRS))
echo ""

# =============================================================================
# CHECK 45: vocab collocation quality — body parts, cross-contamination
# Runs same checks as /audit collocations scope, scoped to this week
# Bug: "leg hurt" (bare imperative), "head ache" (noun-as-verb), grandmother+write
# =============================================================================
echo -e "${BOLD}[CHECK 45] vocab.js collocation quality — body parts, cross-contamination${NC}"
COLL_ERRS=0
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  VOCAB="${dir}/vocab.js"
  [ ! -f "$VOCAB" ] && continue

  python3 -c "
import re, sys
with open('$VOCAB') as f: c = f.read()
entries = re.findall(r'\n\s+\{\s*\n\s+id:\s*\d+.*?word:\s*\"([^\"]+)\".*?collocation:\s*(?:\[([^\]]+)\]|\"([^\"]+)\")', c, re.DOTALL)
issues = []
for m in entries:
    word = m[0]; coll = m[1] or m[2] or ''
    parts = [p.strip().lower() for p in (coll.split('/') if '/' in coll else [coll])]
    wl = word.lower()
    # Body parts: bare imperative 'hurt' (not 'hurts')
    body_parts = ['head','arm','leg','knee','hand','back','shoulder','tongue']
    if wl in body_parts:
        for p in parts:
            if p.endswith(' hurt') or (' hurt' in p and not p.endswith('hurts')):
                issues.append(f'BODY {word}: {p}')
    # Cross-contamination patterns
    if wl == 'grandmother' and 'write' in coll.lower(): issues.append(f'CROSS grandmother')
    if wl == 'teacher' and 'be a teacher' in coll.lower(): issues.append(f'WEAK teacher')
    if wl == 'coast' and 'seaside coast' in coll.lower(): issues.append(f'WEAK coast')
    # Duplicate parts
    if len(parts) != len(set(parts)): issues.append(f'DUP: {word}')
if issues:
    for i in issues: sys.stderr.write(f'  {i}\n')
    sys.exit(1)
" 2>&1
  if [ $? -ne 0 ]; then
    echo -e "   ${RED}❌ FAIL ($LABEL): collocation quality issues — see above${NC}"
    echo -e "   ${YELLOW}   FIX: Body parts use 3rd-person 'hurts' not imperative 'hurt'; no grandmother+write cross-contamination${NC}"
    COLL_ERRS=$((COLL_ERRS+1))
  else
    echo -e "   ${GREEN}✅ PASS ($LABEL): vocab.js collocations clean${NC}"
  fi
done
[ $COLL_ERRS -gt 0 ] && ERRORS=$((ERRORS+COLL_ERRS))
echo ""

# =============================================================================
# CHECK 46: content_vi — no multi-word bold markers in dictation/shadowing/explore
# **phrase** (multi-word) in Vietnamese text = formatting artifact
# **word** (single-word) = functional dictionary cross-ref, OK
# Bug: multi-word **Vietnamese phrase** leaked into content_vi fields (W25-W27)
# =============================================================================
echo -e "${BOLD}[CHECK 46] content_vi — no multi-word bold (**phrase**) in dictation/shadowing/explore${NC}"
MULTI_BOLD_ERRS=0
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LABEL=$([ "$dir" = "$ADV_DIR" ] && echo "Advanced" || echo "Easy")
  for station in dictation shadowing explore; do
    FILE="${dir}/${station}.js"
    [ ! -f "$FILE" ] && continue
    # Extract content_vi lines and find multi-word bold
    BAD=$(python3 -c "
import re, sys
with open('$FILE') as f:
    for line in f:
        if line.lstrip().startswith('content_vi:'):
            for m in re.findall(r'\*\*([^*]+?)\*\*', line):
                if ' ' in m.strip():
                    sys.stderr.write(m.strip() + '\n')
" 2>&1 || true)
    if [ -n "$BAD" ]; then
      echo -e "   ${RED}❌ FAIL ($LABEL ${station}.js): multi-word bold in content_vi:${NC}"
      echo "$BAD" | while IFS= read -r phrase; do
        [ -n "$phrase" ] && echo -e "   ${RED}   → **$phrase**${NC}"
      done
      echo -e "   ${YELLOW}   FIX: Remove ** from multi-word Vietnamese phrases in content_vi (keep single-word **word**)${NC}"
      MULTI_BOLD_ERRS=$((MULTI_BOLD_ERRS+1))
    fi
  done
done
if [ $MULTI_BOLD_ERRS -eq 0 ]; then
  echo -e "   ${GREEN}✅ PASS: no multi-word bold in content_vi${NC}"
else
  ERRORS=$((ERRORS+MULTI_BOLD_ERRS))
fi
echo ""

# =============================================================================
# SUMMARY
# =============================================================================
echo -e "${BLUE}${BOLD}═══════════════════════════════════════════════════════════════${NC}"

# CHECK 20e: read.js — unnatural chunk patterns (adj+profession, doubled modifiers)
# =============================================================================
echo -e "${BOLD}[CHECK 20e] read.js — unnatural chunk patterns${NC}"
UNAT_ERR=0
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LBL=$([ "$dir" = "$ADV_DIR" ] && echo "ADV" || echo "Easy")
  RF="${dir}/read.js"
  [ ! -f "$RF" ] && continue
  TXT=$(grep -m1 "content_en:" "$RF" 2>/dev/null | sed "s/.*content_en.*[\`\"]//" | sed "s/[\`].*//")
  # Unnatural adj+profession
  BADP=$(echo "$TXT" | grep -oiE "(kind|nice|friendly|brave|clever) (chef|scientist|teacher|dentist|artist|policeman|doctor|nurse)" || true)
  if [ -n "$BADP" ]; then
    echo -e "   ${RED}❌ FAIL ($LBL): unnatural adj+profession: $BADP${NC}"
    echo -e "      FIX: use neutral (good cook, science lab, artist...)${NC}"
    UNAT_ERR=$((UNAT_ERR+1))
  fi
  # Doubled modifiers like "all tall trees tall"
  DOUB=$(echo "$TXT" | grep -oiE "(all |big |old |wooden |tall )[a-z]+ \1[a-z]*" || true)
  if [ -n "$DOUB" ]; then
    echo -e "   ${RED}❌ FAIL ($LBL): doubled modifier: $DOUB${NC}"
    echo -e "      FIX: remove duplicate adjective${NC}"
    UNAT_ERR=$((UNAT_ERR+1))
  fi
done
[ "$UNAT_ERR" -eq 0 ] && echo -e "   ${GREEN}✅ PASS: no unnatural chunk patterns${NC}"
echo ""

# CHECK 20f: read.js — redundant bold chunk + standalone word in same sentence
# =============================================================================
echo -e "${BOLD}[CHECK 20f] read.js — redundant chunk + standalone word same sentence${NC}"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LBL=$([ "$dir" = "$ADV_DIR" ] && echo "ADV" || echo "Easy")
  RF="${dir}/read.js"
  [ ! -f "$RF" ] && continue
  TXT=$(grep -m1 "content_en:" "$RF" 2>/dev/null | sed "s/.*content_en.*[\`\"]//" | sed "s/[\`].*//")
  [ -z "$TXT" ] && continue
  ISSUE=0
  # Check each bold chunk: does the word appear standalone nearby after the bold?
  echo "$TXT" | grep -o '\*\*[^*]+\*\*' | while IFS= read -r chunk; do
    WORD=$(echo "$chunk" | sed 's/\*\*//g' | awk '{print $NF}')
    # Check if WORD appears alone (not in a bold) within 20 chars after the chunk
    AFTER_CHUNK=$(echo "$TXT" | sed "s/.*${chunk}//" | head -c 60)
    if echo "$AFTER_CHUNK" | grep -qi "\\b${WORD}\\b"; then
      echo -e "   ${RED}❌ FAIL ($LBL): redundant chunk '$chunk' word '$WORD' reused nearby${NC}"
      ISSUE=$((ISSUE+1))
    fi
  done
  [ "$ISSUE" -eq 0 ] && echo -e "   ${GREEN}✅ PASS ($LBL): no redundant chunks${NC}"
done
echo ""

# CHECK 20g: read.js — verb+adverb masquerading as chunk
# =============================================================================
echo -e "${BOLD}[CHECK 20g] read.js — verb+adverb patterns masquerading as chunks${NC}"
VERBADV_ERR=0
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LBL=$([ "$dir" = "$ADV_DIR" ] && echo "ADV" || echo "Easy")
  RF="${dir}/read.js"
  [ ! -f "$RF" ] && continue
  # Extract bold chunks and check for verb+adverb pattern
  VIOLATIONS=$(grep -oP '\*\*\K[^*]+(?=\*\*)' "$RF" 2>/dev/null | grep -iE '\b(happily|loudly|quickly|slowly|carefully|quietly|warmly|brightly|sadly|angrily|excitedly|hurriedly|carelessly|eagerly|cheerfully|gracefully|gratefully|honestly|politely|seriously|softly|strongly|warmly)\b' || true)
  # Also check "very fast", "run fast", "fell hard", etc.
  VIOLATIONS2=$(grep -oP '\*\*\K[^*]+(?=\*\*)' "$RF" 2>/dev/null | grep -iE '^(run|walk|fell|go|move|turn|jump|swim|drive|fly|dance|sing|play|work|eat|drink|read|write|look|talk|speak|clap|smile|laugh|cry) (very )?(fast|hard|well|high|far|close|quickly|slowly|happily|loudly|carefully|quietly|warmly)\b' || true)
  ALL_VIOL=$(printf '%s\n%s' "$VIOLATIONS" "$VIOLATIONS2" | sort -u | grep -v '^$' || true)
  if [ -n "$ALL_VIOL" ]; then
    echo -e "   ${RED}❌ FAIL ($LBL): verb+adverb bolds found (grammar patterns, not chunks):${NC}"
    echo "$ALL_VIOL" | head -10 | while IFS= read -r v; do
      [ -n "$v" ] && echo -e "   ${RED}   → **${v}**${NC}"
    done
    echo -e "   ${YELLOW}   FIX: Bold the collocation without the adverb (e.g. **sing songs** not **sing happily**)${NC}"
    VERBADV_ERR=$((VERBADV_ERR+1))
  fi
done
[ "$VERBADV_ERR" -eq 0 ] && echo -e "   ${GREEN}✅ PASS: no verb+adverb chunk violations${NC}"
echo ""

# CHECK 20h: read.js — whole sentences/clauses bolded (>5 words, contains verb)
# =============================================================================
echo -e "${BOLD}[CHECK 20h] read.js — whole sentences/clauses bolded${NC}"
WHOLESENT_ERR=0
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LBL=$([ "$dir" = "$ADV_DIR" ] && echo "ADV" || echo "Easy")
  RF="${dir}/read.js"
  [ ! -f "$RF" ] && continue
  # Find bold chunks with 6+ words that contain typical sentence patterns
  VIOLATIONS=$(grep -oP '\*\*\K[^*]+(?=\*\*)' "$RF" 2>/dev/null | awk '{
    n = split($0, w, " ")
    if (n >= 6) {
      # Check if it looks like a clause (has pronoun or determiner at start, or auxiliary)
      if (w[1] ~ /^(I|we|they|he|she|it|my|our|the|there|this|that|but|and|so|when|after|before|from)$/ ||
          $0 ~ /\b(is|are|was|were|can|must|should|will|would)\b/) {
        print $0
      }
    }
  }')
  if [ -n "$VIOLATIONS" ]; then
    echo -e "   ${RED}❌ FAIL ($LBL): whole sentences/clauses bolded:${NC}"
    echo "$VIOLATIONS" | head -8 | while IFS= read -r v; do
      [ -n "$v" ] && echo -e "   ${RED}   → **${v}**${NC}"
    done
    echo -e "   ${YELLOW}   FIX: Bold only the core chunk (e.g. **under the trees** not **are relaxing under big trees**)${NC}"
    WHOLESENT_ERR=$((WHOLESENT_ERR+1))
  fi
done
[ "$WHOLESENT_ERR" -eq 0 ] && echo -e "   ${GREEN}✅ PASS: no whole-sentence bold violations${NC}"
echo ""

# CHECK 20i: content_vi must not contain ANY bold markers (forbidden)
# =============================================================================
echo -e "${BOLD}[CHECK 20i] content_vi — no bold markers allowed (triggers dictionary popups)${NC}"
CVI_ERR=0
STATIONS="read explore dictation shadowing writing grammar vocab"
for dir in "$ADV_DIR" "$EASY_DIR"; do
  [ ! -d "$dir" ] && continue
  LBL=$([ "$dir" = "$ADV_DIR" ] && echo "ADV" || echo "Easy")
  for station in $STATIONS; do
    FILE="${dir}/${station}.js"
    [ ! -f "$FILE" ] && continue
    # Check content_vi section for bold markers
    CVI_BOLDS=$(node -e "
const fs = require('fs');
const src = fs.readFileSync('$FILE', 'utf8');
const m = src.match(/content_vi['\"]?\s*[:=]\s*[\`'\"][\s\S]*?[\`'\"]/);
if (!m) process.exit(0);
const txt = m[0];
const bolds = txt.match(/\*\*[^*]+\*\*/g);
if (bolds) bolds.forEach(b => process.stdout.write(b + '\n'));
" 2>/dev/null || true)
    if [ -n "$CVI_BOLDS" ]; then
      COUNT=$(echo "$CVI_BOLDS" | wc -l | tr -d ' ')
      echo -e "   ${RED}❌ FAIL ($LBL ${station}.js): $COUNT bold markers in content_vi${NC}"
      echo "$CVI_BOLDS" | head -5 | while IFS= read -r b; do
        [ -n "$b" ] && echo -e "   ${RED}   → $b${NC}"
      done
      echo -e "   ${YELLOW}   FIX: Remove ALL ** from Vietnamese text${NC}"
      CVI_ERR=$((CVI_ERR+1))
      break  # One fail per station is enough
    fi
  done
done
[ "$CVI_ERR" -eq 0 ] && echo -e "   ${GREEN}✅ PASS: no bold markers in content_vi${NC}"
echo ""

# =============================================================================
# SUMMARY
# =============================================================================
echo -e "${BLUE}${BOLD}═══════════════════════════════════════════════════════════════${NC}"
if [ $ERRORS -eq 0 ] && [ $WARNINGS -eq 0 ]; then
  echo -e "${GREEN}${BOLD}  ✅ CODE QUALITY GATE PASSED — Week ${WEEK_PAD}${NC}"
  echo -e "${GREEN}  All checks passed. Safe to commit.${NC}"
elif [ $ERRORS -eq 0 ]; then
  echo -e "${YELLOW}${BOLD}  ⚠️  PASSED WITH ${WARNINGS} WARNING(S) — Week ${WEEK_PAD}${NC}"
  echo -e "${YELLOW}  Zero errors (safe to commit). Review warnings before deploy.${NC}"
else
  echo -e "${RED}${BOLD}  ❌ GATE FAILED — ${ERRORS} ERROR(S), ${WARNINGS} WARNING(S) — Week ${WEEK_PAD}${NC}"
  echo -e "${RED}  DO NOT COMMIT. Fix all errors above.${NC}"
  echo ""
  echo -e "${YELLOW}  Reference: CRITICAL_RULES.md | LESSONS_LEARNED_WEEK_9-11_FOR_W12.md${NC}"
fi
echo -e "${BLUE}${BOLD}═══════════════════════════════════════════════════════════════${NC}"

exit $ERRORS
